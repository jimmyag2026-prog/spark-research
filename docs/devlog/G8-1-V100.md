# v0.8 闸门 G-1 · V100 compute uploads 的 workspaceRoot（主会话）

- HTTP/MCP 面：`constrainWorkspaceRoot(requested, projectRoot)`——省略即项目目录；相对路径按项目目录解析；绝对路径只接受项目内；`/etc`、`..` 逃出、项目外一律 400 且消息点名项目目录。CLI 不走这层（用户在自己机器上显式点名）。
- 限额先于读盘哈希：先 lstat 收 size，超 count/bytes 上限就停，不再对越界目录树全量 sha256。

| 门禁 | 阴性对照 → 结果 |
|---|---|
| `v100_upload_workspace.test.ts` ① 越界拒 | 去掉 rel 越界判断 → 1 fail |
| ② 限额先于哈希（含 chmod 000 文件，先哈希会 EACCES） | 把 sha256 挪回收集循环 → 2 fail |
| HTTP `/api/compute/jobs` `/etc` → 400 | — |

unit 2276 全绿（+5）。
