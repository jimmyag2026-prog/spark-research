# A8 · 第七次零上下文验收报告

- 日期：2026-09-15（PDT）
- 被测版本：0.9.0-alpha.3（server `/api/health` 自报）
- worktree：`/Users/jimmyclaw/Desktop/AI4S/spark-research-a8`，分支 `a8/report`
- `git rev-parse --short HEAD`：`cb91648`
- 执行者：零上下文验收 agent（未参与 v0.9 任何 lane，未读 devlog / BACKLOG / DEVELOPMENT_PLAN / USAGE_LOG）
- 截图与原始输出：`/Users/jimmyclaw/Desktop/AI4S/spark-research-a8-artifacts/`

## 网络前提

```
$ for i in 1 2 3 4 5; do curl -s -o /dev/null -m 20 -w "%{time_connect}\n" https://openrouter.ai; done
1.354885
0.175593
0.194658
0.176038
0.174860
```

中位数 0.176s < 1s，最大 1.355s < 3s → **网络达标**，可以出结论。
（已 `unset http_proxy https_proxy all_proxy` 后的直连数。）

## 基线快照（验收前，用于事后还原）

```
defaultModel   = z-ai/glm-5.3-flash   (config)
contactEmail   = jimmyag2026@gmail.com (config)
llmTimeoutMs   = 120000               (default，未写进 config.json)
originAllowlist= —                    (unset)
credentials.json 只含 connectors/aminer/values/api_key（-rw-------）
config.json 实际只有 defaultModel + contactEmail 两个键
```

---
## 行 2 · 网页端换模型 → 发 chat → 台账 `model` 字段一致

**判据**：不一致 = Blocker。**结论：过。**

改前：`defaultModel = z-ai/glm-5.3-flash`（config）。网页端 设置 ▸ 模型 ▸ 默认模型 下拉选 `deepseek-chat`，
`PUT /api/settings/models/default` → `200`，刷新后下拉仍是 `deepseek-chat`，`config.json` 落盘：

```
{"defaultModel": "deepseek-chat", "contactEmail": "..."}
```

随后在网页端新建项目 `a8-20260915` 里发 chat「用一句话说明什么是酶。」（预算填 0.50），台账：

```
$ bun backend/src/index.ts usage --project a8-20260915 --json
  "byModel": { "deepseek-chat": { "calls": 3, "knownCostUsd": 0.0018449200000000001, "unknownCostCalls": 0 } }
```

逐行 ledger（`~/.spark-research/projects/a8-20260915/usage.jsonl`）：

```
{"ts":"2026-09-15T10:08:00.053Z","command":"chat","provider":"deepseek","model":"deepseek-chat","ok":true,"errorKind":null}
{"ts":"2026-09-15T10:08:03.568Z","command":"chat","provider":"deepseek","model":"deepseek-chat","ok":true,"errorKind":null}
{"ts":"2026-09-15T10:08:06.055Z","command":"chat","provider":"deepseek","model":"deepseek-chat","ok":true,"errorKind":null}
```

`model` == 网页端改后的值，`provider`（deepseek）与之匹配，**不是**改前的 `z-ai/glm-5.3-flash`。耗时：换模型 < 3s，chat 端到端 6.1s。

---

## 行 5 · 设置面板逐行对照后端命令 / 无 sandbox / compute 无派发按钮

**判据**：无后端来源的字段 = High；sandbox 或派发按钮 = Blocker。**结论：过（零 High、零 Blocker）。**

设置面共 **12** 个面板（验收表写「8 个」，实物比表多，我按实物 12 个全对了一遍）。
`data-panel` 原文：

```
models / local-models / credentials / sources / scientific-tools / connectors / skills /
compute / network / storage / permissions / general
```

**没有 `sandbox` 面板**（nav 里 12 个 data-panel 全列在上面，无 sandbox）。

逐面板对账（左＝UI 计数，右＝后端命令原文计数）：

| 面板 | UI | 后端来源 | 一致 |
|---|---|---|---|
| 通用 general | `<div class="settings-group" data-count="33">` | `capabilities --json` `.config` = 33；`config list` 33 行 | ✅ |
| 模型 models | 默认模型 + 5 个子代理，候选 17 个模型名 | `capabilities --json` `.providers` = 6；模型清单同 `GET /api/settings/models` | ✅ |
| 凭据 credentials | 5 个 connector（aminer/cnki/semanticscholar/wanfang/modal）+ 7 个 LLM key 位 | `.providers` = 6（+SPARK_LOCAL_LLM_API_KEY） | ✅ |
| 检索源 sources | 8 个文献源勾选 | `config list` 的 `searchSources` 候选 | ✅ |
| 科学工具 | connector 21 + platform 5 + wetBackend 2 + rule 13 | `.connectors`=21 `.simulationPlatforms`=5 `.wetBackends`=2 `.rules`=13 | ✅ |
| 连接器（MCP） | 已装（0） | `ext list` → 「没有已安装的扩展」；`.extensions`=0 | ✅ |
| 技能 skills | 13 个内建技能 | `.skills` = 13 | ✅ |
| 算力 compute | computeTarget 下拉 + local/modal/ssh 三张只读卡 | `compute targets` 三条，文案逐字一致；`.compute.targets`=3 | ✅ |
| 网络 network | originAllowlist/httpTimeoutMs/llmTimeoutMs/contactEmail/userAgent | 同 `config list`（面板自述「与通用面板是同一批值，只是按用途投影」） | ✅ |
| 存储 storage | dataDir / rawLlm / rawUpstreamInline | 同 `config list` | ✅ |
| 权限 permissions | 8 条 withheld 动作 | `capabilities --json` `.mcp.withheld` = 8 | ✅ |
| 本地模型 | SPARK_LOCAL_LLM_BASE_URL / _API_KEY | 同 `config list` | ✅ |

**compute 面板无派发/审批按钮**：面板内只有一个 `computeTarget` 下拉 + 三张只读执行地卡，
面板自述原文：「只改默认执行地。派发（plan / approve / run / release）刻意不走 HTTP——花钱动作由人在终端做（V47 / AD-6）」。
权限面板同样把 `compute_approve` / `compute_run` / `compute_release` 明列为 `withheld`。✅

未发现任何「UI 有但后端命令里找不到同一个值」的字段。

---
