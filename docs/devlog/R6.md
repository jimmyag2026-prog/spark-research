# R6 · 零上下文验收报告（v0.9.0-alpha.2）

- 日期：2026-09-15（PDT 前一日夜间 / 机器本地时区 CST 16:26 起跑）
- 被验版本：`v0.9.0-alpha.2`，worktree `r6/report`，commit `680df65`
- 执行者：零上下文子代理（未参与 v0.9 任何 lane），只读 `R6_A8.md` / `T5_config_ops.md` / `docs/taskbooks/v0.6/T1–T4`
- server：`http://127.0.0.1:4321`，`{"status":"ok","service":"spark-research","version":"0.9.0-alpha.2"}`（pid 46563，启动于 15:25:05，cwd `/Users/jimmyclaw/Desktop/AI4S/spark-research`，非本 agent 启动）
- 原始输出与截图：`/Users/jimmyclaw/Desktop/AI4S/spark-research-r6-artifacts/`

## 网络前提（R6 本轮自测一次）

```
$ for i in 1 2 3 4 5; do curl -s -o /dev/null -m 20 -w "%{time_connect}\n" https://openrouter.ai; done
0.426454
0.186051
0.188973
0.178366
0.180427
```

中位数 **0.186s**（< 1s）· 最大 **0.426s**（< 3s）→ **达标**。

## 基线（主会话已跑，本报告引用不复跑）

引自 `docs/devlog/R6-baseline.md`（2026-09-15T08:23:41Z，server 0.9.0-alpha.2，固定消息 × 每项目 5 轮）：

| 项目 | 墙钟 P50 | 墙钟 P90 | 调用/轮 中位 | 调用/轮 最大 | 失败轮 | errorKind 分布 |
|---|---:|---:|---:|---:|---:|---|
| t1-protein-r3 | 97.3s | 213.2s | 3 | 4 | 0/5 | — |
| t2-sc-r3 | 101.4s | 287.2s | 3 | 5 | 0/5 | — |
| t3-bci-r3 | 26.9s | 69.9s | 3 | 4 | 0/5 | — |
| t4-pero-r3 | 31.5s | 35.2s | 3 | 4 | 0/5 | — |

基线里已登记 U11（`/api/session/chat` 不读 `?project=`）、U12（预算闸拒绝返 HTTP 200）、U13（单轮 > 255s 被 `idleTimeout` 掐断）。本报告的新发现**从 U14 起编号**。

### 执行环境的一处前置（如实记录）

本 worktree 是全新检出，**没有 `node_modules`**，`bun backend/src/index.ts doctor` 首次直接报
`error: Cannot find module '@modelcontextprotocol/sdk/server/index.js'`。
我把主检出的 `node_modules` 软链进来（`ln -s /Users/jimmyclaw/Desktop/AI4S/spark-research/node_modules node_modules`，未修改任何受版本控制的文件）后一切正常。这不是缺陷，但记下来，因为它是零上下文执行者遇到的第一个硬停。

---

# T5 · 配置与运维（预算 $0.50）

新建项目 slug：`t5-20260915`。

## 第 1 步 · `doctor`

```
$ bun backend/src/index.ts doctor
Spark Research v0.9.0-alpha.2 · doctor（2026-09-15T08:27:08.279Z）
…
▎运行实例（探端口 4321）
  ✅:4321    版本一致（v0.9.0-alpha.2）
      pid 46563 · 启动于 Tue Sep 15 15:25:05 2026
      bun backend/src/index.ts server 4321
      cwd /Users/jimmyclaw/Desktop/AI4S/spark-research
▎前端
  ❌ 未构建  /Users/jimmyclaw/Desktop/AI4S/spark-research-r6/frontend/workspace/dist
      修复：bun run build:web
```

耗时 0.21s。全文见 `artifacts/T5/`（终端回显）。

**判断：过。**「运行实例」一档存在，报了端口 / 版本一致 / pid / 启动时间 / 命令行 / cwd，足以判断是不是孤儿。
附带观察（不计缺陷）：`doctor` 的「前端未构建」判的是**当前 cwd 的 dist**，而不是那个正在跑的实例实际服务的 dist；在 worktree 里跑 doctor 会得到「实例健康 + 前端未构建」这种自相矛盾的一屏。见 U16。

## 第 2 步 · 起 server，浏览器打开工作台

server 已在跑（按任务说明不重起 4321）。Playwright headless 打开 `http://127.0.0.1:4321/`，截图 `artifacts/T5/step2-workbench.png`。

顶栏原文：`Spark Research | speed-probe | 0 record · 0 文献 · 0 思路 | 导出报告 | v0.9.0-alpha.2`

**判断：半过。**
- 版本徽标 `v0.9.0-alpha.2` 存在 → 过。
- **当前项目是 `speed-probe`（「模型速度对比（一次性）」，项目描述原文「对比实测，用完可删」），正是验收产物** → 不过。项目下拉框里 35 个项目全部可见，包含 `binary-probe` / `v27probe` / `a5-accept` / `r4-t1…r5-t4-copy2` / `speed-probe` / `r6-probe` 等成套验收与探针产物，一个都没被折叠。「显示已归档」开关存在但默认态下这些项目就已经在列表里。→ 归档折叠对既有验收产物没生效（U14）。

## 第 3 步 · 设置面板与 `config list` 逐字对照

`config list` 输出存 `artifacts/T5/step3-config-list.txt`；设置面板全文存 `artifacts/T5/step3-settings.txt`，截图 `step3-settings.png`。

对照结果：
- 面板「通用」列出的键与 `config list` 的 33 行**一一对应，说明文字逐字一致**（抽查 `defaultModel`「所有 LLM 调用的默认模型（provider 由模型名推断）」、`searchSources`「不给 `--sources` 时默认查哪些文献源（逗号分隔的 connector id）」、`rawUpstreamInline`「凭据协议源（AMiner/CNKI/万方）的原始响应体是否 inline 存进 raw/connector/」均一致）。
- 凭据类项（`KIMI_API_KEY` / `OPENROUTER_API_KEY` / `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` / `DEEPSEEK_API_KEY` / `QWEN_API_KEY` / `SPARK_LOCAL_LLM_API_KEY`）在通用面板**只有「已配置 / 未配置」与一句指引**，DOM 里对应位置**没有 input**（我把页面上所有 `input/select/textarea` 全枚举了一遍，见 `step3-settings.txt` 的 `=== INPUTS ===` 段，29 个输入控件里没有一个属于这些键）。面板顶部还写明「凭据类项在本面板只读，写入走「凭据」面板」。
- 左侧有独立的「⚿ 凭据」面板。

**判断：过。**

附带观察：`config list` 把 `searchSources` 的值截断成 `openalex,crossref,europepmc,sema` 且**不加省略号**，网页端显示的是完整值 `openalex,crossref,europepmc,semanticscholar,arxiv,pubmed,biorxiv`。照 CLI 抄会抄到一个错的值。见 U17（P2）。

## 第 4 步 · 改 `defaultModel`

改前（原值，最后要改回）：`z-ai/glm-5.3-flash`。
候选来自 `GET /api/settings/models` 的 `allowed`（17 个），挑 openrouter 家的 `moonshotai/kimi-k2.6`（`providers.openrouter.configured: true`）。

网页端设置 ▸ 通用 ▸ `defaultModel` 输入框填入 `moonshotai/kimi-k2.6`，点「保存」：

```
[XHR] PUT 200 http://127.0.0.1:4321/api/settings/general/defaultModel
[XHR] GET 200 http://127.0.0.1:4321/api/settings/general
input value before: z-ai/glm-5.3-flash
input value after reload: z-ai/glm-5.3-flash      <-- 刷新后仍是旧值
```

文件确实写进去了：

```
$ cat ~/.spark-research/config.json
{
  "defaultModel": "moonshotai/kimi-k2.6",
  "contactEmail": "jimmyag2026@gmail.com"
}
$ bun backend/src/index.ts config get defaultModel
defaultModel
  当前值: moonshotai/kimi-k2.6
  来源:   env（环境变量 SPARK_RESEARCH_MODEL 可覆盖）
```

但同一时刻 server 仍然返回旧值：

```
$ curl -s http://127.0.0.1:4321/api/settings/general | …
{"key": "defaultModel", "value": "z-ai/glm-5.3-flash", "source": "env", "configured": true,
 "nextStep": "当前值来自环境变量 SPARK_RESEARCH_MODEL——在这里改会写进 config.json，但要 unset 掉那个环境变量才会生效"}
{"key": "contactEmail", "value": "jimmyag2026@gmail.com", "source": "env", "configured": true,
 "nextStep": "当前值来自环境变量 SPARK_RESEARCH_CONTACT_EMAIL——在这里改会写进 config.json，但要 unset 掉那个环境变量才会生效"}
```

而 `SPARK_RESEARCH_MODEL` **根本没被设置**——我的 shell 没有，server 进程也没有：

```
$ if [ -n "${SPARK_RESEARCH_MODEL+x}" ]; then echo YES; else echo NO; fi
NO
$ ps eww -p 46563 | tr ' ' '\n' | grep -cE '^SPARK_'
0
（server 进程 40 个环境变量里一个 SPARK_ 前缀都没有）
```

**判断：不过。**两条：保存后刷新仍是旧值（U15）；`source` 把「来自 config.json」标成 `env` 并指引用户去 unset 一个不存在的环境变量（U15 同条）。

## 第 5 步 · 新建项目 + 发一条 chat

网页端「＋ 新建项目」→ slug `t5-20260915`、标题「T5 配置与运维」→ `POST 201 /api/projects`，顶栏与下拉框立刻切到 `t5-20260915`。
composer 填「用一句话说明什么是酶。」，预算 `0.30`，发送。

等待期的进度文案（每 3s 采样，`artifacts/T5/step5.txt`）：

```
[+3.0s]  规划中：正在拆解任务
[+6.1s]  规划完成：共 1 个任务
[+42.4s] （进度区为空）
[+57.5s] （开始出正文）
```

回复正文（节选，截图 `step5-final.png`）：

```
research agent · chat · 08:31:53
[session web_1789461107887] [analysis] t2: 完成 — 续接执行日志中断处的综合结果
一句话定义
酶是由活细胞产生的、化学本质绝大多数为蛋白质（少数为催化性 RNA 的核酶）的生物催化剂，…
```

**判断：半过。**有回复 → 过。进度「带计数」只在规划段出现（「规划完成：共 1 个任务」），**执行段没有 `execute 1/1` 这类计数**，而且 +42s 到 +57s 之间进度区是空的（既不是计数也不是文案），约 15s 没有任何反馈。比「一句不动的文案」好，但没达到任务书写的「带计数的分段进度」。见 U18（P1）。

## 第 6 步 · 台账 `model` 字段对不对得上（**P0**）

```
$ bun backend/src/index.ts usage --project t5-20260915 --json
{
  "project": "t5-20260915",
  "calls": 3,
  "inputTokens": 1783,
  "outputTokens": 4027,
  "knownCostUsd": 0.0012033578999999998,
  "unknownCostCalls": 0,
  "unpricedCalls": 0,
  "noUsageCalls": 0,
  "byErrorKind": {},
  "byCommand": { "chat": { "calls": 3, "knownCostUsd": 0.0012033578999999998, "unknownCostCalls": 0 } },
  "byModel": { "z-ai/glm-5.3-flash": { "calls": 3, "knownCostUsd": 0.0012033578999999998, "unknownCostCalls": 0 } },
  "corruptLines": 0
}

$ cat ~/.spark-research/projects/t5-20260915/usage.jsonl
{"ts":"2026-09-15T08:31:59.803Z","command":"chat","provider":"openrouter","model":"z-ai/glm-5.3-flash","ok":true,"inputTokens":705,"outputTokens":225,"costUsd":0.0001151205}
{"ts":"2026-09-15T08:32:33.916Z","command":"chat","provider":"openrouter","model":"z-ai/glm-5.3-flash","ok":true,"inputTokens":595,"outputTokens":2419,"costUsd":0.0006851967}
{"ts":"2026-09-15T08:32:55.455Z","command":"chat","provider":"openrouter","model":"z-ai/glm-5.3-flash","ok":true,"inputTokens":483,"outputTokens":1383,"costUsd":0.00040304069999999993}
```

期望 `model == moonshotai/kimi-k2.6`（第 4 步改后的值），实得 `z-ai/glm-5.3-flash`（**改前**的值），三条全是。`provider` 恰好都是 openrouter，所以只看 provider 发现不了。

**判断：不过 → P0（U15）。** 这是 U10「换模型没真换」的**同形复现**，只是换了发生位置：v0.8 是 `chat()` 不读 `req.model`，本轮是**网页端改了模型、文件也写对了，但 server 进程用的还是启动时的旧值**。
