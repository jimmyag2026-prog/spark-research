# A6 · 第五次零上下文验收 + alpha.7 修复

> 报告：`~/Desktop/AI4S/spark-research-v0.7-plan/A6/A6_report.md`。判定：**可发布**，无 Blocker/High，1 Medium（V88）、4 Low（V89/V90/verified 语义/OpenAlex 429）。

## 验收者没看出来但我复核出来的（V91）

验收者把 `data import` 的 `verified:false` 读成「没顺带校验」。用它的 `export_full` 目录重新导入：journal ✅，
raw/connector arxiv 第 5/5 行断链、raw/llm 第 59/59 行断链。回到验收者的源项目核：**源项目的链本来就断**，
第 59 行 prevHash = 第 57 行 hash——两个进程（server 上的 novelty-check 任务 + CLI）同时 append，各自按进程
缓存的 lastHash。修法：读尾 + 文件锁；`data import` 逐链逐项报告。

## 门禁与阴性对照

| 门禁 | 结果 |
|---|---|
| `raw_append_race`（两进程各 100 次） | 200 行链完整；阴性对照（缓存 + 无锁）→ 红 |
| `w7a6_r4_fixes` V91 用例（同 ts 五行往返） | 绿 |
| 用 A6 真实导出再导入 | 照样报 ❌ 两条链断（源数据如此，正确） |

## V58 口径

这是验收之后改了被验收的东西。改动面 = raw sink 的 append 临界区 + import 的报告形状，窄验收 =
上表三条 + 六套件；不重跑整套 A6。
