# Backlog

> Single registry: out-of-scope / pending items are all recorded here, not scattered across devlog entries.
> ℹ️ **Numbering note (2026-09-11)**: V83–V91 = v0.7 R4/A6 reconciliation; **V92–V103 = v0.6.0 external review reconciliation** (originally registered as V83–V94 on branch `docs/v0.6-external-review`; renumbered +9 on merge; source of truth `docs/reviews/v0.6.0_external_review.md`).
> **v0.6.0 external review reconciliation (2026-09-11, source of truth docs/reviews/v0.6.0_external_review.md)**:
> After item-by-item empirical verification, newly registered as V92–V103; items already disposed of by v0.7 W7 are not re-registered (P0-1 half of the pointer issue = #74, P2-4 = #69,
> most of P2-12 = #74, N2 = #63, P1-4 = V78 previously registered); two places where the review was inaccurate are honestly noted (the claim that state has "no lock" does not hold
> — the lock already existed in the reviewed version; N2 did not reproduce across 3 consecutive runs on current main, since it was already fixed by #63).
> **This is not a table that only grows and never shrinks** — every item must have a disposition: absorbed into some phase, explicitly deferred, or explicitly declined with a reason.
> **v0.6.0 external review reconciliation (2026-09-11, source of truth docs/reviews/v0.6.0_external_review.md)**:
> After item-by-item empirical verification, newly registered as V83–V94; items already disposed of by v0.7 W7 are not re-registered (P0-1 half of the pointer issue = #74, P2-4 = #69,
> most of P2-12 = #74, N2 = #63, P1-4 = V78 previously registered); two places where the review was inaccurate are honestly noted (the claim that state has "no lock" does not hold
> — the lock already existed in the reviewed version; N2 did not reproduce across 3 consecutive runs on current main, since it was already fixed by #63).
> Last updated: evening of 2026-09-11 (v0.8 plan committed to the repo: added a v0.8 row to the disposition summary table, back-tagged R1/R3/S12/V57/V83; merged the v0.6.0 external review reconciliation and renumbered to V92–V103; v0.7 alpha.7: A6 passed, newly registered V88–V91, V91 fixed; alpha.6 R4 fix window: newly registered V83–V87, V80's root cause fixed, V74's systematic fix; alpha.5: V64/V65 residual/V71/V72 closed, V67's deep pool done, awaiting R4 for T2/T4 numbers; W7-D2: data export/import/verify landed, V82's wiring closed; W7-D1: V24/V30 closed, V82's disposition set to D2; alpha.2 close-out: V69/V60/V70/V3/V41/V48 closed (each with residuals), V21's deprecation cycle started, V67's ranking done with depth pending a decision, V50 ruled rev +4; W7-D0 close-out: newly registered V82; V80 closed; V62 ruled exempt; V78's main body fixed, residual registered; baseline gate: newly registered V81 and fixed; v0.7 plan committed to the repo: added a v0.7 row to the disposition summary table; back-tagged 7 items already completed but not marked in the table — V1/V7/V11/V15/V17/V18/V19; V16 ruled closed; V12 supplemented with GLM 0% measurement data);
> Earlier the same day: (back-registered V76: the 154-skill / 29-connector inventory staged in the v0.5 planning directory, which previously had no pointer anywhere in the repo;
> Same day: v0.6 W6-1 close-out reconciliation: V9/V28/V29/V43① closed, V53/V54/V56 closed, V16/V50 partially handled and archived, R3/A5 wrap-up added V77–V80; added V61–V63; R1-findings reconciliation: fixed V64's minimum defense line + completed S1, added V64–V74;
> Previous round: 2026-09-10 (v0.5 planning kicked off: V4's launch condition triggered, V2 has a design draft, added V26;
> the v0.5 planning source of truth lives in the local directory `~/Desktop/AI4S/spark-research-v0.5-plan/TODO_v0.5.md`, deliberately not checked into the repo, to be reviewed after v0.4 is complete)

## Disposition Summary Table (see at a glance where every item stands)

The v0.3 plan (`DEVELOPMENT_PLAN_v0.3.md` §8 addendum) has reconciled/settled all items into the backlog. This is the reverse index:

| Disposition | Items |
|------|------|
| **v0.3 will do** | V1 V3 V7(deleted) V8 V9 V11 V12 V13 V14 V15 V16 V17 V18 V19 · D2 |
| **After v0.3** (below, §post-v0.3) | V2 V4 V5 V6 V10 · D1 D3 |
| **Absorbed** | G1–G7 (P8) · three MCP friction points (v0.2.1) |
| **v0.8 reconciled** (`DEVELOPMENT_PLAN_v0.8.md` §10, user 2026-09-11: everything actionable goes in) · **alpha.2 status**: Gate G's six items ✅ · α β γ δ ε ζ all merged ✅ · V102 split into V104–V117 · residuals V118 V119 V120 | Gate G: V100 V101 V93 V94 V96 V21 · α: V67 V86 V87 V73 · β: V85 V78 V63 V97 V99 V98 · γ: V88 V89 V90 V79 · δ: V92 V13 V14 V51 · ε: V95(software) V55 V59(software) · ζ: V2 · Decided: V12 V102 V42 · Awaiting external: V4 V5 V6 V10 V52 D1 D2 D3 V76 |
| **Still open at v0.7.0 release** (goes into v0.8's first batch) | V67/V86 recall retest (needs contactEmail) · V85 simulation not in raw · V83 real-directory pollution disposition (waiting on user) · V88 V89 V90 A6 UX items · V87 citation-judgment count discrepancy · V78 residual subAgentLlm · V60/V70 residuals |
| **v0.7 reconciled** (`DEVELOPMENT_PLAN_v0.7.md` §11) | Do: V24 V30 V41 V48 V60 V62 V63 V64 V67 V69 V70 V71 V72 V78 V80 V3 V21 · Ruled closed: V12(after R4) V13 V16 · Registered as won't-do: V14 V42 · Back-tagged as already completed in this PR: V1 V7 V11 V15 V17 V18 V19 |
| **In v0.5 planning** (local planning directory, not checked in) | V2 V4 V15 V25(field materialization) V26 · plus new items such as connector/skill/inline-view extensions, see planning directory TODO_v0.5.md<br>**The 154-skill / 29-connector inventory staged in that directory has been registered as V76** |
| **v0.8.1** (`DEVELOPMENT_PLAN_v0.8.1.md`, patch release, 2026-09-13 external review) | Gate H (done): V134 V135 V136 V137 V138 · investigated, downgraded (architecture conflict, not a bug — see V139 row): V139 · Gate H (do): V140 V141 · narrative correction (do): V143 V144(a) V145 · open decision (user): V142 · deferred to hardware-integration gate: V144(b) V146 · already tracked elsewhere, no new registration: V147(=V67/V86 recall) |

---

## post-v0.3 · to be revisited after v0.3 is complete

> User directive 2026-09-09: these wait until v0.3 development is done before continuing.
> Three categories: **waiting on real demand** (building it now means nobody uses it), **waiting on a prerequisite** (doing it now means rework), **waiting on external input** (not in our hands).

### Waiting on real demand — only do it once there's an actual use case, otherwise it's spinning wheels

| # | Item | Why wait | When to start |
|---|-----|---------|-----------|
| V4 | Real implementation of remote compute (Docker / SSH / Modal / Volcano Engine) | The abstraction layer is in place, but no real research task requires running remote yet; implementing it speculatively risks building to an imagined use case | Once a research task's compute needs exceed what the local machine can provide<br>✅ **Launch condition triggered (2026-09-10, user decision, set as the v0.5 main line)**: connection design complete (planning directory `workstreams/compute/COMPUTE_DESIGN.md`, following the OpenScience JobBroker architecture: Plan-summary approval + durable-volume results as source of truth + symbolic credentials + three-axis lifecycle; first implementation is Modal, with SSH/Volcano Engine left as Target slots). On the v0.4 side, only the interface reservation in P12 §4.2 item 6 |
| V5 | R kernel | A slot has long existed in the permit set, but so far no one has asked for R; cheaper to add one more kernel after P12 makes subagents real | Once a user explicitly asks for R |
| D1 | A third simulation platform (materials computation VASP/LAMMPS? EDA?) | The `SimulationPlatform` contract has already been validated for generality by two implementations, OpenMM + pyref. Once P15's `ext verify` lands, it's more cost-effective for third parties to add their own than for us to do it for them | Once a user's real research task specifies a platform |

### Waiting on a prerequisite — doing it now would mean rework or wouldn't hold up

| # | Item | What prerequisite it's blocked on |
|---|-----|-------------|
| V2 | Semanticizing novelty similarity (literal wording → embedding) | Needs an embedding-provider decision (self-hosted? go through an LLM provider?). P11's provider abstraction paves the way for it — doing it after that's paved means not doing it twice. **Currently known risk**: literal-wording threshold of 0.75, calibration sample of only 2 claims, nearest-neighbor margin of 0.08<br>**2026-09-10 addendum**: research confirms OpenScience has **no** embedding abstraction (the upstream `embeddingModel` is explicitly commented out and disabled), so there's no reference implementation to copy; a v0.5 design draft is ready (planning-directory provider workstream: `llm/embeddings.ts`, an abstraction on par with chat, backend preferring local zero-key Ollama with OpenAI reusing the existing key, literal-wording method retained as a fallback path with dual-trace logging per AD-8). Landing it still waits on P11<br>✅ **v0.8.0-alpha.2 closed** (#94 ζ: semantic routing/literal-wording fallback/threshold recalibration was already delivered back in v0.5 W5-1; this version adds embedding cost accounting + CLI/HTTP wiring; qwen embedding has no official unit price and is not included) |
| V6 | Physical Opentrons / real device integration | The hard prerequisite is v0.3's D-8 "safety-gate promise made good." Touching physical devices before the safety gate can actually hold is irresponsible. Connecting to the same device family works via a drop-in new `WetLabBackend`; a **non-Opentrons device family** requires pushing the device-language compilation down into the backend (build instructions in `EXTENDING.md` §4) |
| V10 | Real identity at the HTTP layer (multi-user scenario) | v0.3 makes the agent layer real first. Currently, the actor for `approve` is "whoever claims to be who" — honest for single-user local use, **must be resolved before going multi-user**, otherwise approval auditing doesn't hold up. Needs to pair with V19 (approval requires an interactive terminal, done in v0.3 P12) to be complete |
| V15 | Remove deprecated aliases such as `MCPConnector` | v0.3 P10 will attempt this; can only delete once confirmed there are no external references. If it can't be done, it stays here |

### Waiting on external input — not in our hands

| # | Item | Waiting on what |
|---|-----|--------|
| D3 | Real CNKI / Wanfang API | Whether the user has an API channel. No public API exists; AMiner remains the main path for Chinese-language literature |

---


## P8-gate (pre-release verification checklist) — **fully absorbed (P8, 2026-09-09)**

| # | Item | Source | Result |
|---|-----|------|------|
| G1 | E2 conclusion-card review gate: pending/approved/vetoed, only approved enters the report's conclusions section | DESIGN E2; P5 only landed a minimal conclusion record | ✅ `conclusion/` module + three entry points (CLI/API/UI); the ruling is non-negotiable (any hard finding → vetoed) |
| G2 | E1 data-conclusion consistency checker (an observation cited by a conclusion must actually exist in the execution record) | DESIGN E1 | ✅ `data-consistency`; all four adversarial cases (fabricated / deleted / cross-project / wrong type) are hard |
| G3 | E1 statistical-plausibility soft-hint checker | DESIGN E1 | ✅ `stats-plausibility`; only emits soft findings, tagged `heuristic:true`, includes a negative control |
| G4 | Capability-flag consumers: absorbing `deterministic` (range vs. byte-for-byte reconciliation) and `simulated` (a mocked reading of 0.0 must not be treated as real data in a conclusion) into the E1 checker and the P8 report | P5/P6 review-pass annotations | ✅ `capability-labeling`; both an unlabeled simulation and a claim of byte-for-byte reproducibility on a non-deterministic platform are hard; report wording changes with the capability flag |
| G5 | Measuring the judge's precision/recall for "real key, fake content" (Pattern B) under a real model | P3 devlog annotation | ✅ Measured (three difficulty tiers, 5 runs): precision 100% across all tiers and all runs; easy/medium recall 100%; hard recall 71–100%. See devlog for details |
| G6 | Delete the deprecated `compute/providers.ts` and its v0.1 tests | P5 review-pass annotation | ✅ Three files under `backend/src/compute/` plus their tests deleted, no remaining references |
| G7 | Evidence graph → Markdown report export + the research-report skill (§5.3's 10th skill) | PLAN P8 | ✅ `report/export.ts` + `report` CLI + `/api/report` + a workbench export button + the research-report skill |

## Full Item Details (in registration order)

> Each item's **disposition** is in §Disposition Summary Table; deferred items have their rationale expanded in §post-v0.3.
> This section preserves the original registration content and source — it's traceable history and is not deleted.

| # | Item | Notes |
|---|-----|------|
| V19 | Approval actions require an interactive terminal | A corollary of the AD-9 ruling: the MCP layer blocks the default path and accountability, **not** a technical workaround. To truly block "an agent using Bash to call `lab approve --actor <a made-up name>`," the CLI layer needs to require that approval come from a TTY, or require a confirmation token that a non-interactive environment can't obtain. That's the real technical defense; until then, don't claim approval "cannot be automated"<br>✅ **Completed (v0.4 P12, back-tagged this round)**: `lab/cli.ts:55` `approvalIsInteractiveTty` TTY probe + interactive-confirmation read, comment directly tags V19 |
| # | Item | Notes |
|---|-----|------|
| V14 | Change location-weighting exemptions to an explicit allowlist | P3 D3 said "refactor once there's a second exception"; P8's three new rules became exceptions all at once, so the threshold has been reached. Currently isolated via the call path, which works but isn't explicit<br>✅ **Changed to an explicit allowlist in v0.8.0-alpha.2** (#95: explicit `LOCATION_WEIGHT_EXEMPT` list) |
| V1 | Integrate arXiv/PubMed into unified search | Missing an XML parser; unified search currently has only 4 JSON sources<br>✅ **Completed (v0.4 W3-d, back-tagged this round)**: `literature/models.ts:10-11`'s default source set includes arxiv/pubmed; CHANGELOG: "arXiv / PubMed integration (BACKLOG V1) — XML parsing via a TS extension" |
| V2 | Semanticize novelty similarity (embedding-provider decision + integration) | Literal-wording threshold 0.75 with a margin of 0.08; see alongside G5 |
| V3 | Cross-verify process start-time in polling | Current state: the "done.json takes priority" direction is safe, it can only over-report `running`<br>✅ **v0.7 alpha.2 C-2 did half of it — "pid reuse"** (PR #67): simulation submit now records the child process's start time, and poll does pid-identity cross-verification on the no-live-handle path. **Residual**: "pid does not exist" still falls through to the existing `failed` (folding it into `orphaned` would touch `experiment/loop.ts`'s SIGKILL assertion — filed as its own item together with the V70 residual) |
| V4 | Real implementation of remote compute (Docker/SSH/Modal/Volcano Engine) | DESIGN explicitly states this is demand-driven |
| V5 | R kernel | A slot already exists in the permit set left over from v0.1 |
| V6 | Physical Opentrons / real device integration | Needs real hardware. Same device family = drop-in with a new WetLabBackend; a **non-Opentrons device family** requires pushing the "structured steps → device language" compilation down into the backend (currently `execute()`'s input parameter is `OpentronsProgram`). **P9 has already written the five-step build instructions into [EXTENDING.md §4](EXTENDING.md)** (including the migration of protocolHash semantics and the fallback wording for the volume_capacity rule); hold until a second device family is chosen (AD-4's lesson: it takes two real implementations to validate an interface) |
| V7 | Integrate Agent Swarm (a v0.1 legacy) into the new architecture | Evaluate together with per-subagent model configuration<br>✅ **Completed (deleted in v0.4 P12, back-tagged this round)**: `swarm.ts` no longer exists; CHANGELOG: "deleted swarm (330 lines, v0.1 legacy, zero production callers)" |
| V8 | Optimize Chinese search-query recall | P4 measured extremely poor recall for Chinese search queries<br>✅ **Mechanism located and fixed (see V65, v0.6)**: root cause = AMiner's title search matches by word-sequence. After adding a word-splitting fallback, multi-concept Chinese queries went from a total miss to usable; further recall improvements fall under V67 (ranking) and introducing a tokenizer (not scheduled) |
| V10 | Real identity at the HTTP layer (multi-user scenario) | P7 current state: `approve`'s actor is "whoever claims to be who" (actorSource=`http:explicit`). Honest for single-user local use; **must be replaced with real identity authentication before going multi-user**, otherwise approval auditing doesn't hold up |
| V11 | Persist long-task handles to disk | P7 current state: `server/tasks.ts`'s task list is lost on process restart (the experiment state on disk is still there, and `exp run --resume` can reconnect). Persisting to disk is needed if the UI should see "tasks currently running" across a restart<br>✅ **Completed (v0.4 P14, back-tagged this round)**: `server/tasks.ts` snapshots to disk; v0.5's V35 and v0.6's V68 build on top of it. CHANGELOG: "persist long-task handles to disk (V11)" |
| V9 | **[Completed]** Verify AMiner's `getPaper` detail endpoint with a real key | `search` has already been verified for real (HTTP 200)<br>✅ **Verified in v0.6 G-4**: with a real key, `getPaper` measured HTTP OK, with complete detail fields (abstract/authors/venue/year). Key renewal (expires 2026-10-07) remains a separate ops item; the user has explicitly stated it's out of scope for this version. |
| V12 | LLM structured output (`response_format` / JSON mode) | P8-G5 measured: kimi-k2.6 has 2–6% of judgments per round that only emit chain-of-thought with no JSON in `content` (`finish_reason` isn't `length`, so it's not truncation). A "retry once on parse failure" has been added, but it treats the symptom not the cause. A real fix means either having LLMRouter support `response_format: json_object`, or configuring the judge with a model that supports structured output. The current state is safe (degrades to a visible `citation_judge_unavailable` soft finding rather than pretending to pass), at the cost that this batch of citations goes unchecked this round<br>📐 **v0.7 ruling candidate (supplementary data)**: across three rounds R1–R3 on `z-ai/glm-5.3-flash`, the judge's JSON failure rate was **0%** (CHANGELOG v0.6.0's "numbers" section; R1 T1+T3, R2 T2+T4, R3 all four topics in full). The 2–6% above is kimi-k2.6's number. **v0.7 R4 reran one topic on a second provider and still got 0% → closed**; otherwise `CallOptions.responseFormat` would go into v0.8. See `DEVELOPMENT_PLAN_v0.7.md` §5 |
| V13 | The judge prompt's wording on "attribution out of thin air" | P8-G5 measured: the one stable false-negative pattern in the hard tier is "the draft gives an attributed explanation not present in the card." The prompt says "claiming a result the card explicitly doesn't have = conflict," but the model reads it as "the card doesn't cover it → unclear." Both readings are defensible — it's an ambiguity in the prompt, not a model error. Before tightening the wording, think through whether it would also sweep in legitimate summarization (exactly the kind of false positive P3 deliberately avoided)<br>✅ **Ruled closed in v0.8.0-alpha.2** (#95: item-by-item review of R4's four-topic drafts found not a single case of attribution out of thin air; the prompt is not changed; rationale and false-positive risk in devlog W8-delta) |

| V15 | Remove deprecated aliases such as `MCPConnector` | P9 already renamed it to `HttpConnector` (along with `HttpConnectorConfig` / `HttpTool`) and kept three aliases for backward compatibility with external references. Can delete in v0.3 — but must first confirm no external integration is using the old names<br>✅ **Completed (v0.5.0 Gate F-3, back-tagged this round)**: zero references to `MCPConnector` repo-wide; CHANGELOG: "the deprecation cycle ran its course with v0.4.0; deleted after confirming zero live references repo-wide" |
| V16 | Expose per-subagent model configuration as a user-facing setting | Current state: `SubAgentConfig.model` is a code-level setting, and everything falls back to `LLMRouter.DEFAULT_MODEL`. The payoff of "strong model for heavy tasks, fast model for retrieval/summarization" should be evaluated together with V7<br>⚠️ **v0.6 G-1 did the user-visible half**: the `defaultModel` setting is now genuinely read at all three entry points (CLI/HTTP/agent) (the CLI previously didn't read it at all — the 8th instance of the V40 shape), and the main LLM commands got a `--model` override. A per-"subagent-type" model configuration item already existed as of v0.4 (subAgentModel_*); the joint evaluation with V7 is still pending.<br>🔒 **Ruled closed in v0.7**: the user-visible half (`defaultModel` genuinely read at all three entry points + `--model`) was done in v0.6 G-1; the "joint evaluation with V7" has no object since V7 was deleted; the `subAgentModel_*` setting already existed as of v0.4. No longer pending |
| V17 | Progress reporting for long-running MCP tasks | Current state: before timeout, the MCP side only polls the task handle, and progress isn't reported back to the client. The MCP protocol has progress notifications — once wired up, an external agent can see "reading paper 7/20 in depth" instead of waiting blind<br>✅ **Completed (v0.4 P14, back-tagged this round)**: 9 progress-notification sites under `backend/src/mcp/`; CHANGELOG: "MCP progress reporting (V17)" |
| V18 | Cache `capabilities --probe` results | Current state: every probe spawns a child process (once each for openmm/opentrons, on the order of seconds). An external agent repeatedly calling `research_capabilities(probe=true)` would wait for nothing. The cache must carry an invalidation condition (venv changes), otherwise it will lie<br>✅ **Completed (v0.4 P14, back-tagged this round)**: probe cache carries a venv-invalidation criterion; CHANGELOG: "probe cache carries a venv-invalidation criterion (V18)" |
| V20 | `backend/src/index.ts`'s `CONFIG_DIR` doesn't honor `SPARK_RESEARCH_DATA_DIR` | P10 lane D-c found: `config/index.ts`'s `dataDir()` honors this environment variable, while `index.ts`'s `CONFIG_DIR` is hardcoded to `~/.spark-research`. Harmless under default settings, but it means a temp workspace injected by tests has no effect on the `auth` command — two code paths resolve the same config.json two different ways<br>✅ **Absorbed in v0.4 P11 lane R-d**: `index.ts`'s `CONFIG_DIR` now calls `config/index.ts`'s `dataDir()`, so the two paths resolve the same config.json consistently; also cleaned up the now-dead `homedir` import and the hardcoded path in `auth`'s success message. |
| V21 | Inconsistent prefix for timeout-related environment variables | The four timeouts newly registered during P10's close-out used `SPARK_HTTP/LLM/KERNEL/TASK_TIMEOUT_MS`, while the repo's existing convention is `SPARK_RESEARCH_*` (e.g. `SPARK_RESEARCH_MCP_TIMEOUT_MS`). **Not aligning this during P10 was deliberate**: v0.2.1's MCP tool descriptions had already published `SPARK_TASK_TIMEOUT_MS` to external agents, so renaming is a breaking change that must go through a deprecation cycle<br>⚠️ **v0.7 alpha.2 E-3 started the deprecation cycle** (PR #68): the new names `SPARK_RESEARCH_{HTTP,LLM,KERNEL,TASK}_TIMEOUT_MS` take effect; the old names emit a one-time warning when read; if both old and new are set, the new one wins; MCP descriptions synced. **v0.8 removes the old names**<br>✅ **v0.8.0-alpha.1 old names removed** (#89, devlog G8-6: reading one now errors out) |
| V22 | **`capabilities` broadcasts a skill externally that can't actually be called (the self-description surface is lying)** | P10's D-12 gate found that `backend/src/proteins/analysis.ts` is referenced only by tests; further verification during v0.4 planning found this is **worse than "one fewer entry point"**: `capabilities --json` broadcasts `protein-analysis` in full as an **available capability** — with a description, `triggers` ("what does this protein look like," "is there an available structure"), a connector list, and a `validation` file list — while **all three entry points, CLI / HTTP / MCP, have none** (measured: the only one of 10 skills like this; its SKILL.md is also the only one with no CLI example). An external agent reading the triggers would be certain it can call this. **This isn't a completeness gap, it's the self-description surface lying to the outside world** — a direct violation of AD-12 ("capability claims must be machine-verifiable"), and the existing gate only checks for orphaned modules, not skill reachability. Side note: the second item in its listed `validation`, `tests/integration/protein_record.test.ts`, is one of 8 `skipIf(!RECORDING)` test cases that has never run this round. Disposition per v0.4's P11 lane R-d: either add real entry points or pull it from the skill catalog — pick one, don't leave it hanging<br>✅ **Absorbed in v0.4 P11 lane R-d**: `protein-analysis` got three real entry points added — CLI `spark-research protein <query>`, HTTP `POST /api/proteins/analyze`, MCP tool `protein_analyze` (MCP tool count 29 → 30); SKILL.md got the CLI example it was previously the only one missing. **And the acceptance criterion was turned into a gate check**: `narrative_parity.test.ts`'s 7th assertion checks that every skill has at least one reachable entry point, cross-checking each registry entry against three sources — the `switch(cmd)` case literals in `index.ts`, the `MCP_TOOLS` array, and the `capabilities` skills list (not relying on prose regexes). A negative control was independently rerun by the main session: removing `case "protein":` → the gate immediately went red. |
| V23 | Wet-lab `unconsumedWarnings` is not yet force-displayed on the HTTP / Web approval surface | P10 lane D-d delivered: the CLI's compile and approval output forcibly display it; the HTTP/UI side isn't wired up yet. **Must be added before connecting physical devices** — otherwise someone approving via the Web won't see the part that says "you wrote it, but the safety gate never saw it"<br>✅ **Absorbed in v0.4 P11 lane R-d**: the HTTP layer was already structurally passing it through (`viewJson()` spreads the whole `WetExperimentView`), it just wasn't tested; the real gap was in the frontend — the `WetExperiment` type was missing the `unconsumedWarnings` field, and the approval modal never rendered it. Added the type + a prominent warning block (`data-testid="unconsumed-warnings"`) + an HTTP assertion + e2e ⑨b. Negative control: forcibly hiding the warning block → e2e ⑨b goes red. |
| V24 | `RecordIntegrityError` has no recovery path | The record integrity hash check added in P10 lane D-d can only refuse to trust a record it detects has been tampered with — there's no "repair after manual confirmation" entry point, only manual handling via `RecordStore`'s raw interface<br>✅ **Done in v0.7 W7-D1**: `records_journal` (append-only, prevHash chain) + `RecordStore.repair(id, {toSeq, actor})` rebuilds the projection from the log, CLI `report records --repair <id> --to-seq N --actor X`. After an integrity-check failure, it's no longer refuse-only — a human confirms and signs off on a rebuild, logged as op=repair |
| V25 | **[Completed]** Natural-language parsing for `concentration_limit` / `biosafety` | P10 D-8 explicitly left undone: the two rules run permanently idle on the main pipeline, backstopped only by `unconsumedWarnings` alerts. **This is a hard prerequisite for V6 (connecting physical Opentrons)**, see README's "current real coverage of the safety gate" <br><br>✅ **Done in v0.5 W5-1 δ (partial coverage, boundaries honestly documented in README)**: the two rules went from permanently idle to genuinely consuming input. But they only handle the case where "concentration/BSL appears **in the same sentence** as the target reagent or step"; the cross-sentence phrasing (reagent stated first, concentration stated afterward — the most common form) has the compiler **refuse to guess the attribution** and it still falls to unconsumedWarnings. **The V6 hardware threshold is not lifted by this.** |
| V26 | Pool connector rate limiting by host | **Newly registered 2026-09-10** (source: v0.5 connector-expansion research). Current state: the connector layer only has politeness headers (`politeness.ts`), **there is no rate limiter of any kind**. pubmed and ncbi already share NCBI eutils' host-level budget, and the ClinVar / GEO connectors planned for v0.5 hit the same host — four connectors each doing their own thing will collectively get 429'd. If a rate limiter is built, it must be keyed and pooled by host, not by connector. Disposition: do it in the same batch as v0.5's eutils-family connector integration <br><br>✅ **Done in v0.5 W5-2 γ**: `backend/src/http/ratelimit.ts`, a token bucket, **keyed strictly on `new URL(url).host`**. `HOST_RATE_POLICIES` only registers `eutils.ncbi.nlm.nih.gov` (3 rps / burst 3, per NCBI's official documentation) — because it's the only one shared by multiple connectors this wave (pubmed/ncbi/clinvar); no rps figure with a traceable source could be found for reactome / string-db, so **none was fabricated**. Negative control actually run: switching to per-connector keying → 40 concurrent requests land in one window (cap 32); independently rerun by the main session, confirmed 3 red. |
| V27 | **[Completed]** **`import.meta.dir` is broken across the board in the single binary (blocks npx distribution)** | v0.4 W1 close-out measured: in the artifact produced by `bun build --compile`, `import.meta.dir` points at the virtual `/$bunfs/root/` — **any code that builds paths from it can't read any files inside the binary**. Currently affects **23 sites across 17 files** (`kernels/manager.ts` looking for Python scripts, `lab/wet_backend.ts` looking for `opentrons_backend.py`, `agents/*` looking for prompt `.txt` files, `simulation/*` looking for adapter scripts, `skills/frontmatter.ts`, `scaffold/templates.ts`, `server/app.ts` looking for frontend build output…). Two sites have been fixed (`index.ts` and `version.ts` switched to static imports, plus a version-number-consistency assertion was added); **the rest are unfixed, and the consequence is that wet/dry experiments, prompt loading, scaffolding, and frontend hosting are likely all broken inside the binary**. **W1 close-out measurement (worse than the initial assessment)**: `bun build --compile` **does not embed non-JS assets** (`.sql` / `.py` / prompt `.txt`), so every command inside the binary that needs to read an asset gets ENOENT —
```
$ ./dist/spark-research project new x   → ENOENT: /$bunfs/root/schema.sql
$ ./dist/spark-research doctor          → /$bunfs/root/opentrons_backend.py: No such file
$ ./dist/spark-research lit sources     → works fine (pure TS, doesn't read assets)
```
That is to say, **the binary can list connectors but can't create a project** — it can't do any real work.<br>**This is a hard blocker for v0.4's "one npx command" delivery**, and must be resolved before shipping v0.4.0, or explicitly downgrade the promise (ship only the npm package, not a single binary). Fix directions: Bun's embedded files, switching assets to static imports, or unpacking to a writable directory at runtime. <br><br>✅ **Done in v0.5 W5-1 ε + close-out**: 10 asset sites (3 `schema.sql` via static import · 4 `.py` embedded as text + unpacked and spawned at runtime · 3 prompt `.txt` files) + 3 dangerous default-path sites. Full smoke test passed on the real binary in a clean directory (`project new` / `lit search --add` / `doctor` / `exp run --platform pyref`, pyref error 3.8e-9). Close-out also patched one last piece: `capabilities --json` used to report "0 skills" inside the binary (directory enumeration, which static imports don't cover) → each SKILL.md embedded individually, **measured 0 → 10**. |
| V28 | **[Completed]** The binary artifact has no smoke test | All of W1-d's tests run against `bun backend/src/index.ts` — **the binary is a different runtime** — so issues of V27's kind can't be caught by them (the version-number mismatch was only found because the main session manually ran the binary). The build only takes about 300ms, so it's worth adding a CI smoke test: build → three paths, `--version` / `capabilities --json` / `doctor` → assert the version number is consistent across all three and isn't `0.0.0`.<br>✅ **Done in v0.6 G-2**: `scripts/smoke-binary.sh` (version consistent across three places + capabilities skill count + server starts in a clean directory with the homepage returning 200 and the bundle reachable) + the first GitHub Actions CI (unit tests + smoke test). **Caught a real bug on day one of going live** (V59), proving this gate isn't just for show. |
| V29 | **[Completed]** npm packaging fields incomplete | W1-d originally planned to do `package.json`'s `files` / `engines` / `prepublishOnly`, but after the main session's budget was cut short, its remaining scope was narrowed to "just fill in documentation," so this part **was neither completed nor silently dropped** — W1-d honestly recorded it as a TODO in `docs/INSTALL.md`. It needs to be filled in for the `npx spark-research` path to actually work.<br>✅ **Done in v0.6 G-2**: files/engines/prepublishOnly all in place; dist ships with the package (works out of the box with the UI). Whether to actually publish to npm is up to the maintainer. |
| V30 | **The "delete a paper" path is unreachable today, and the orphan-reconciliation scan is called only by tests** | W4-b delivered `retractOrphanRecords()` per E-6 (marking records that point at a deleted paper as `retracted`), but close-out review found two things: ① `LibraryStore.remove()` has **zero production callers** — users can't delete a paper at all today, so the "delete a paper, leave an orphan" scenario is unreachable; ② `retractOrphanRecords()` is also referenced only by tests. The narrative gate can't see this because orphan detection operates at **file granularity**, and `reading.ts` as a whole has production callers (the expressiveness ceiling noted back in W3-c). **Disposition: any future delete entry point must wire up this reconciliation scan at the same time**; or turn it into a maintenance command to make it reachable. No new entry point before release — that would be a rushed change with no lane-testing discipline behind it.<br>✅ **Done in v0.7 W7-D1**: `LibraryStore.remove()` changed to a tombstone (`removed_at`, the row stays, re-add revives it); production caller `lit remove <id|doi>`, the same action runs `retractOrphanRecords()` (now going through the narrow `records.tombstone()` interface, logged as op=tombstone). The hard-delete path no longer exists |
| V31 | Execution records for external MCP tools don't enter the evidence graph | W4-d made every external tool call log a record (all four branches — success/failure/timeout/unknown tool — log one, pinned down by a negative control), but the record is written to `extensions/<name>/.mcp_calls.jsonl`, **it never enters the project's evidence graph** (`project/**` is outside that lane's ownership). So the differentiator relative to OpenScience — "external tool calls naturally enter provenance" — **is only half delivered**: the audit record exists, the evidence graph isn't wired up yet. Don't claim full delivery in external materials before it's wired up. <br><br>⚠️ **v0.5 W5-2 δ built the mechanism but didn't wire it into production** (δ proactively disclosed this): `mcp_client.ts`'s optional `recordSink` field logs an `observation` (`kind:"external_tool_call"`, `evidence:"sourced"`), and all four branches (success/failure/timeout/unknown tool) log one; `contract.ts` excludes it from the evidence graph (**a hard requirement of AD-10** — an external call is an audit trail, not progress; counting it as evidence would invalidate the `noProgress` stop condition, which is exactly the shape of P12's `agent_run` incident, and δ built a named regression test for it). **But no caller in production passes `recordSink`.** The main session traced this further and found it's even more thorough than that: `connectExternalMcp()` has **zero callers across the entire production path**, only `ext verify` uses it — the flow "agent connects to an external MCP extension at runtime" **simply doesn't exist in production**. See V45. <br><br>✅ **Closed in v0.5 W5-3 γ** (one asymmetry to disclose: the trace only covers "the call happened"; "the connection failed" has no tool call to trace, so it only enters the execution log, not the evidence graph). |
| V32 | ToolBus not yet swapped over to the external tool runner | W4-d provided `createExternalToolRunner()` (returns an instance directly assignable to `AgentToolBus.options.runner`), but close-out never wired it up — once wired, subagents could actually call external MCP tools and automatically get the same authorization/budget/audit treatment. Two halves of the same thing as V31. <br><br>⚠️ **Same as V31, mechanism complete, not wired into production**: `orchestrator.ts`'s `OrchestratorDeps.externalTools` injection slot is open, and injecting it routes through `createExternalToolRunner()`. But none of the three construction sites — `index.ts:339,388` and `server/context.ts:124` — pass it in. See V45. <br><br>✅ **Closed in v0.5 W5-3 γ + close-out**. After γ wired up the execution chain, **it exposed its own gap**: `AgentToolBus.specs()` is `MCP_TOOLS.filter(...)`, so names with the `mcp:` prefix could never enter the tools list visible to the model — **a real model won't spontaneously think to call a tool it was never told exists**. Close-out added the missing `externalSpecs` pass-through chain (`ExternalToolRegistry.specs()` had carried a comment saying "for close-out to wire in" since W4-d, and **it waited two full waves**). External tools also go through the grants allowlist the same way; not granting one = byte-for-byte identical behavior to before wiring. |
| V33 | **[Completed]** **In the binary, `workspaceRoot`'s default value resolves to the filesystem root `/workspaces`** | Found during the v0.5 Gate F-c investigation, verified by the main session: `agents/orchestrator.ts:223`'s `join(import.meta.dir, "../../../workspaces")`, in the compiled artifact, equals `join("/$bunfs/root", ...)` → **`/workspaces`**, immediately followed by `mkdirSync(workspaceRoot, { recursive: true })`. **This isn't an ENOENT, it's writing to the filesystem root** — blocked by permissions on macOS, but would really create it if run as root on Linux. In the V27 family but more serious (the rest are "can't read a file," this one is "writing to the wrong place"). Must be handled together when fixing V27. <br><br>✅ **Done in v0.5 W5-1 ε**: `workspaceRoot` now hangs off `join(dataDir(), "workspaces")`; the other two dangerous paths were changed to explicit refusals in the compiled artifact. Verified on the real machine: after running `server`, `<dataDir>/workspaces` gets created, and `/workspaces` does not exist. |
| V34 | **[Completed]** **`DEFAULT_SEARCH_SOURCES` never caught up with W3-d's arxiv/pubmed integration** | **Found by measurement during zero-context external acceptance review (v0.5 Gate F-1), verified by the main session**: `literature/models.ts:16-21`'s default sources are still the P2-era `[openalex, crossref, europepmc, semanticscholar]`, **not including arxiv / pubmed**. So `lit search --sources arxiv` works, and `capabilities` even reports arxiv as available, but `lit add <arxiv-id>` uses the default value to query four sources that don't include arxiv → reports "not found." **The capability was built, the default just never followed.** AD-12's gate can't catch this category — it checks "is arxiv in the registry," not "does the default set include it." While fixing the default, consider adding a gate check: **an already-implemented source must either be in the default set, or have an explicit exclusion reason**. <br><br>✅ **Done in v0.5 W5-1 ζ**: the default set now includes arxiv/pubmed (6 total now). **The important part is the other half** — a new `tests/unit/literature_source_parity.test.ts` treats the connector registry as the source of truth (treating a hand-written copy of `LITERATURE_SOURCES` as the source of truth would be self-certifying), and the only legitimate exclusions recognized are `apiKeyRequired` or `placeholder`. The gate immediately caught two sources nobody had thought of (cnki/wanfang were in the registry but not in the union type). Verified on the real machine: `lit add 1706.03762` without `--sources` went from "not found" to a successful import. |
| V35 | **[Completed]** **Long-running tasks are completely invisible at the CLI layer** | The **top blocker in the external acceptance review**: `lit read --all` produces zero output for 8 minutes, no progress, no job id, and there's no way to check status after disconnecting — the reviewer could only kill the process and use `lit list` to infer after the fact that it had actually been working. **Yet the MCP layer has a `task_status` mechanism, and W4-c already added long-task persistence and MCP progress reporting (V17)** — the CLI is the only entry point not wired up. This is a CLI/MCP capability imbalance, not a missing feature. <br><br>✅ **Done in v0.5 W5-1 ζ**: no new capability needed, just wiring — `server/tasks.ts` already has all the capability, the CLI was the only unwired entry point. Added `backend/src/cli/progress.ts`; `lit read --all` / `lit review` now go through the task handle, and a new `lit tasks [<id>] [--json]` reads the persisted snapshot, so status can be checked after a disconnect/restart. Two deliberate decisions: **no default timeout** (introducing a new failure path in the name of visibility would be backwards), and `--json` silences progress output. |
| V36 | **[Completed]** Failure messages give no next step | External acceptance review: both `idea new`'s contract-validation failure and `lit add`'s "not found" **only say what went wrong, not what to try next**, so the reviewer had to guess their way around it. Counter-example: `lab approve`'s V19 rejection message gives complete next-step guidance — a good template. <br><br>✅ **Done in v0.5 W5-1 ζ + η**: written to V19's quality standard. ζ's guidance is derived from what actually happened this run (identifier shape → which source should parse it → whether that source is in this search's source set), and it splits `skipped` further into "missing credentials" vs. "doesn't recognize this id shape" — the two call for different actions, so collapsing them into one sentence would just push the judgment call back onto the user. η added a next step to `idea new`'s contract-validation failure. |
| V37 | **[Completed]** `auth` reports a different status than `config list` / `doctor` for the same key | External acceptance review: `sr auth` reports `OPENROUTER_API_KEY` as unconfigured, while `config list` / `doctor` correctly show it as configured. **Three places reading the same config give contradictory answers** — the same category of problem as the hand-written `PROVIDER_API_KEY_ENV` copy fixed during P11 close-out (the source of truth was never unified). <br><br>✅ **Done in v0.5 W5-1 η + close-out**. The root cause was more specific than reported: `index.ts:103` had a **hand-written `KEY_NAMES` copy listing only 2 providers** (6 are actually implemented), and `auth()`'s display **only read the config file, never the environment variables**. Changed to derive from `PROVIDER_API_KEY_ENV` and label the source, and added a regex assertion to prevent the hand-written table from recurring. **Close-out also fixed a deeper issue**: `defaultProvider` is a setting that is **written but never read** (see V40). |
| V38 | **[Completed]** BibTeX export mis-parses author names in "Last F" form | Found by external acceptance review, causing incorrect citation-key generation. <br><br>✅ **Done in v0.5 W5-1 ζ**: criterion is "the last segment is judged an initial only if it consists of 1–3 uppercase letters" — requiring uppercase is the key, so `Jan van der Berg` / `Xu Li` aren't mistakenly caught. **Also fixed a more subtle issue in passing**: `dedupe.ts`'s cross-source surname comparison was also misaligned (openalex side `jumper` vs. europepmc side `j`), because it goes through the same `firstAuthorSurname`. |
| V39 | **[Completed]** `lit review --help` runs the command instead of showing help | Found by external acceptance review. Broken help is an onboarding problem. <br><br>✅ **Done in v0.5 W5-1 ζ**: 9 subcommand-level help entries, intercepted uniformly **before** the switch (putting it inside each case would miss any newly added subcommand). While writing this gate, ζ hit the same shape itself: the first version checked `flags.h`, but `-h` is a single dash and gets swallowed by `parseFlags` as a positional argument, so `lit export -h` still ran the command. |
| V40 | **`defaultProvider` is a setting that is written but never read** | **Found and fixed on the spot during v0.5 W5-1 close-out** (registered here to remember the shape, not as a to-do). `auth()` lets the user pick a default provider and persists it, `config set` can set it, and `auth` even echoes it back — **but no code anywhere uses it to select a provider**; `getApiKey()` just takes the first one with a key present, in declared order. If a user picks kimi, as long as `OPENROUTER_API_KEY` is also present it silently goes through openrouter instead, leaving no trace. **This is the project's 7th instance of "built but no production caller," but the previous six were orphaned modules that the gate could catch — this one is hiding inside a config setting** — the gate checks "does the module have a caller," not "does the config setting have a reader." Worth adding a future gate: a writable config setting must have a reader. |
| V41 | **No gate covers capability claims in MCP tool descriptions** | v0.5 W5-1 γ's third negative control found: changing `chem_depict`'s MCP description to falsely claim support for 3D docking **turned no test red**. MCP tool descriptions are **capability claims shown to external agents**; the consequence of them lying is the same as `capabilities --json` lying, but AD-12's machine-verifiability only covers the latter. γ honestly recorded this without quietly patching it (outside its footprint).<br>✅ **Done in v0.7 alpha.2 E-1** (PR #68): `narrative_parity`'s 9th check — capability words in MCP tool descriptions (3D/docking, full text, real search/real-time, GPU, remote) are individually verified against the source of truth; negative control: `chem_depict` falsely claiming 3D docking → red. Also corrected the `protein_analyze` description in passing |
| V42 | **The local compute target's `network` field is a declaration, not an enforcement** | v0.5 W5-1 α honestly recorded: when `adapters/local.ts`'s `capabilities().network` declares `"none"`, the local process can in fact still reach the network (no sandbox / netns). **Name doesn't match reality.** α chose to record this rather than pretend it's isolated. The real remote target (Modal) is unaffected. |
| V43 | **The frontend directory and scaffolding are still unusable inside the binary** | The remaining piece of V27, honestly listed by ε: ① `server/app.ts:27`'s frontend directory resolves to `/frontend/workspace/dist` in the artifact (it's read-only with an `existsSync` guard so it won't crash, but `server` comes up with no UI); ② `new skill|connector|platform` and `ext verify --kind platform` have been changed to an **explicit refusal** in the artifact (rather than silently doing the wrong thing) — **`docs/INSTALL.md` should state explicitly that these two only work from a source checkout**, which ε did not fix (outside its footprint).<br>✅ **v0.6 G-2 closed ①**: the frontend dist is now embedded via build-time codegen and unpacked/hosted via runtime materializeAssetTree; the binary's `server` opens straight into the workbench (pinned by smoke test ③). ② scaffolding retains its explicit refusal and this is written into INSTALL.md §3 — **this is a deliberate end state**, scaffolding needs to write into the repo's source tree, and there's no legitimate meaning for it inside a binary |
| V44 | `semanticscholar` is flagged `apiKeyRequired` yet stays in the default set (ruled to keep it) | A wording/policy question raised by ζ, **ruled by the main session: keep it**. The reason is a substantive difference from aminer — `literature.ts:748` records "P2 measured: anonymous requests get sustained 429s," meaning that without a key it goes to **`skipped` (with configuration guidance, visible)**, not a silent empty result; removing it from the default set would leave users who do have a key with one fewer source. Registered here **so the next person knows this asymmetry is intentional**, not an oversight. |
| V45 | **The flow "agent connects to an external MCP extension at runtime" doesn't exist in production** (the real gap behind V31/V32) | **Found during v0.5 W5-2 close-out investigation, more thorough than δ's report**: the only callers of `connectExternalMcp()` and `discoverExternalMcpTools()` are `ext verify` / the `ext` CLI — **not a single one on the agent's runtime path**. So the two optional parameters V31/V32 added (`recordSink` / `externalTools`) have no production caller — not because someone "forgot to pass it," but because **there is no such flow to pass it through**.<br>What needs to be built is the full lifecycle: discover installed `mcp_client` extensions → connect when the agent starts running (**child process**) → register into `ExternalToolRegistry` → bind the project's `recordSink` → tear down at the end → **a broken extension must not be allowed to take down the whole run**. Wiring points: `extensions/loader.ts` · `daemon/daemon.ts` · `server/context.ts:124` · `index.ts:339,388`.<br>**Main session's judgment: this isn't close-out work, it's a lane** — hand-rolling child-process lifecycle management during close-out is exactly the kind of cross-layer change engineering discipline rule #13 warns against. **Scheduled into W5-3**. Until it's done, it is **not permitted** to claim externally that "external tool calls naturally enter provenance." <br><br>✅ **Done in v0.5 W5-3 γ**: a six-stage lifecycle (discover → connect → register → bind recordSink → finally tear-down → per-extension failure isolation), implemented in `loader.ts` to avoid a `loader ⇄ mcp_client` circular dependency (W4-d got bitten by TDZ once already). Three layers of laziness: for a user with no `mcp_client` extension installed, the entire cost is one extra readdir when they type `chat`. **During the investigation, found a second "flow nobody walks"**: `runResearchLoop()` itself also has zero production callers — both were wired up. |
| V46 | **Modal credential field names once mismatched between two lanes** (fixed, registered for the shape) | Found during v0.5 W5-2 close-out measurement: β wrote `token_id`/`token_secret` in the configuration guidance per design doc §1.1.7, while α read `tokenId`/`tokenSecret` following the Modal SDK's `ModalClientParams` (verified against the `.d.ts`). **The result is that a user who fills things in exactly per the prompt gets a permanent "unconfigured" from the adapter, and goes back to repeatedly check their own token.** Changed to derive from the adapter's `MODAL_REQUIRED_CREDENTIAL_KEYS` and added a gate.<br>**Registered to remember the shape**: this is the **third time**, after V34 (default sources) / V37 (auth wording), that "the same thing has two hand-written copies," and this time it's **two parallel lanes each writing their own copy** — a single lane's gate can't see the other lane. Parallel development needs extra defenses against this category. |
| V47 | The HTTP surface deliberately has no compute dispatch / release endpoints | A design decision from W5-2 β, written into `/machine`'s `httpWithheld`: MCP is a projection of HTTP, **no HTTP route = no path exists no matter what the withholding table says**, part of AD-14's defense-in-depth. The cost is that **the Web workbench can't click to dispatch** — the user has to go back to the CLI. **Main session ruling: keep the current state for v0.5** (compute just landed and there's only local, so the conservative direction is right), but this is a genuine UI dead-end, and re-adding it in the future needs fresh justification (which would open the path for MCP at the same time). |
| S1 | **`lit add` degrades one id shape into another and collides with the library** (✅ fixed) | **The top finding of the zero-context external acceptance review at the end of W5-2, reproduced by the main session on the real machine**: `lit add 9999.99999` (a nonexistent arXiv id) imported a 1978 paper, "Intravenous nitroglycerin," **reporting ✅, exit code 0**. pubmed's eutils leniently parsed it as PMID 9999. **This isn't "not found" being reported as "looked and it's not there" — it's "not found" being reported as "found it, here's a different paper for you."** A junk paper ends up in the evidence graph, `lit read` spends real money generating a close-reading card for it, it gets listed as a reference in `report export`, and `citation-integrity` waves it all through (the papers really are in the library). <br>The root cause is the **fourth instance of "the same thing has two hand-written copies"**: `search.ts` hardcodes a three-source allowlist, while `cli.ts` already has the correct `SHAPE_SOURCES` capability table, used only for display text. Changed to a single source of truth + gate + negative control.<br>**Something more important the negative control exposed**: with the fix removed, this import pulled in a bioRxiv paper instead — **every source added widens the blast radius by another ring**. So the gate must be pinned to a general-purpose judgment, not patched source by source. |
| S2 | **Compute outputs don't enter the evidence graph** | External acceptance review hit a dead end at the last step of the main flow: `compute run` / `collect` succeed, but `report stats` is all zeros, the `execution_records` table is empty, and the harvested `result.json` just sits in `jobs/<id>/harvest/` without being registered as an artifact. Cascading consequence: `conclusion`'s rule that "an observation cited by a conclusion must actually exist in the execution record" **can never pass for compute results** — **it's currently impossible to write a single reviewable conclusion based on a compute run**.<br>**This wasn't a planning gap**: design §1.1 always assigned it to **W5-3 α** ("one observation (`kind:"compute_output"`) + one artifact record per harvest file"); acceptance review hit it because W5-3 hadn't run yet. The README already honestly documents this limitation. <br><br>✅ **Done in v0.5 W5-3 α, three things independently verified by the main session**: `observation|computed|compute_output`, `artifact|computed|compute_output_file`, and the `derives_from` edge between them. The write point is placed in `broker`, not the CLI — collect has both a CLI and an HTTP entry point, and writing in only one would silently fail to log evidence via the other (the V46 shape). Using `runId=jobId` as the execution anchor is the other key half (`conclusion_rules`'s `evidence_without_execution` reads it). **The reviewer's line, "it's impossible to write a single reviewable conclusion based on a compute run," now goes all the way through to `✅ approved (0 hard / 0 soft)`.** |
| S10 | `report export`'s "Appendix A · Evidence Index" is an empty table | The same report elsewhere states "5 papers · 5 close-reading cards," while the evidence index table is empty — a reader would simply conclude the report is broken. Either fill it in, or explain when it's empty that "this table only indexes evidence cited by the body text." Assigned to W5-3 δ. <br><br>✅ **Done in v0.5 W5-3 δ**, and **without blindly filling the table**: first diagnosed that Appendix A's semantics were always "only index records the body text actually cites" — the empty table wasn't broken, the label just didn't say so clearly (changing the semantics would break an existing test's assertion that "recordIds == the body-text citation set"). Kept the semantics + added a scope note + pointed to `report records`. |
| S11 | **No CLI command can view a raw record** | To answer "did this actually enter the evidence graph," the reviewer tried `report stats` (only 9 category counts, no artifacts), `report export` (index empty, see S10), and guessed the command names `records`/`record`/`graph` (none of them right), **ultimately had to open sqlite themselves**. For a product that treats `records.db` as a core selling point, this is a glaring hole. Assigned to W5-3 δ. <br><br>✅ **Done in v0.5 W5-3 δ**: `report records [--type] [--limit] [--json]` and `report show <id>` (including inbound and outbound edges). **Deliberately extended `report` rather than creating a new `records` namespace** — settling three points of namespace contention at once. Verified by the main session: output matches a direct `sqlite3` query line for line. |
| S12 | A number of minor inconsistencies | The `delivery` status doesn't match `llms.txt`'s description (it says collect only happens when pending, but in reality `none` works too and is even suggested) · unexplained jumps in the `rev` number · after a local task finishes harvesting, `resource=active` dangles and `release` is never suggested · `chem depict`'s human-readable output gives no file path (`--json` has `path`) · `--gpu`'s error on local, "(optional: none)," is oddly worded and gives no next step. Assigned to W5-3 δ. <br><br>⚠️ **v0.5 W5-3 close-out did 4/5**: the documentation was wrong (`mcp/tools.ts` said collect only happens when delivery=pending, actually it's none/pending/failed, three states) · collect not suggesting a next step causing `resource=active` to dangle · `chem depict`'s human-readable output missing path · `--gpu`'s error on local giving no next step. **The 5th item, the `rev` jump, was not fixed** — δ diagnosed the mechanism (CAS increments on every write) but explicitly said "no confident fix," only offering two candidate directions. See V50.<br>✅ 4/5 done (v0.5 W5-3 close-out), the 5th folded into V50 and ruled archived — closed with this round's back-tagging |
| V48 | **The local compute handle isn't persisted mid-execution → the design's SIGKILL acceptance path doesn't work today** | **Honestly disclosed by v0.5 W5-3 α (it couldn't fix this, outside its footprint)**: if the orchestrating process is killed mid-run → `job.json.adapterHandle` is null → **it can neither be reconnected to nor harvested**. The original message, "this task was never actually dispatched," **is false**, and leaving `recoverable` at false would let `release` delete the only copy of the output — α made the message honest, but a real fix requires changes to `target.ts` / `adapters/local.ts`. **The acceptance path written in design §1.1.9, "SIGKILL → resume → harvest," does not hold on local today**; must not be claimed externally.<br>✅ **Done in v0.7 alpha.2 E-2** (PR #68): `RunHooks.onHandle` — on a successful local spawn, the handle is written back immediately; a real SIGKILL only kills the orchestrating process → the new process's recover reconnects and harvests. Design §1.1.9's acceptance path now holds on local. Cost: one extra patch during dispatch, the rev jump goes from +3 to +4 (see V50) |
| V49 | **The wording/policy for `deterministic=true` is pending a ruling** (user decided 2026-09-11 to defer) | W5-3 β measured: the three platforms scanpy / pydeseq2 / cobrapy are **byte-for-byte reproducible on repeated runs on the same machine** (the runner pins n_jobs/n_cpus to 1, fixes seeds, cobrapy defaults to pFBA), but **doesn't guarantee this across machines / across BLAS implementations / with a different solver**. The staged design draft originally wrote `false`; β changed it to `true` based on measurement and set up a gate that re-tests every time.<br>**The issue is one of wording/policy, not of fact**: if "reproducible across machines" is the bar for calling something deterministic, all three of these labels need to be flipped together, and `pyref` (the only pre-existing `deterministic=true`) would need re-review too. This label is **an input to the determinism layer**; mislabeling it lets a model's conclusion sidestep the constraint.<br>**β's 4th negative control also dug up something deeper**: "deterministic mislabeled" **could not turn red at all** before `embedding.csv` was added — with the PCA seed removed, not a single byte of clusters/markers changed. **When the output contains no data sensitive to randomness, this label is unobservable at the test layer.** |
| V50 | Unexplained jumps in the `rev` number | The 5th item from external acceptance review S12. δ diagnosed the root cause as CAS incrementing on every write (`job_store.ts:170` + multiple call sites in `broker.ts`); a new plan comes out at rev 2, jumps to 5 after a run completes, and to 7 after collect completes. **δ explicitly stated it had no confident fix**, only offering two candidate directions (expose only a monotonic count externally / separate the internal write count from the user-visible rev). This is a product judgment call, left for the next version.<br>⚠️ **v0.6 W6-1 γ partially addressed**: merged two patches within dispatch into one (jump reduced from +4→+3, recorded in CHANGELOG). **Field-level separation explicitly not done**: CAS (approval.ts) depends on rev incrementing +1 per write, and existing tests pin this semantic; separating them would require making approval/cli part of the same project, and the payoff doesn't match the risk. Archived here for now; revisit if external acceptance review flags it again<br>📐 **v0.7 alpha.2 close-out ruling**: V48's early handle-persistence added one more patch within dispatch, bringing the rev jump back to +4. "Recoverable from a crash" takes priority over "jumping by one less"; `w61_cli_polish_rev.test.ts` was honestly updated with a note. Field-level separation is still not done |
| V51 | Shared helpers are in an awkward location + three probes are still in an inline shape | Disclosed by W5-3 β: `probeCodeFor`/`datasetParam` live in `simulation/scanpy/probe.ts`, with the other two platforms doing `import ../scanpy/probe` — **they really belong at the top level of `simulation/`; this shape was forced by file ownership**, and moving them is a pure relocation. Also: the probes for `doctor.ts` / `pyref` / `openmm` are still in an inline-string shape (**the old "probe says it's available but actually submitting a task breaks" shape**, which bit the project once back in V27), and β verified the fix is reusable but it's outside its authorized scope.<br>✅ **Put back in place in v0.8.0-alpha.2** (#95: `simulation/probe.ts` now at the top level, scanpy/pydeseq2/cobrapy share it; openmm residual → V118) |
| R1 | **The wet-lab compiler rewrites reagent identity** (🔴 fixed) | **The top blocker in the pre-release zero-context external acceptance review (clean machine, binary only), reproduced by the main session on the real machine**: writing "sulfuric acid" compiles to "**hydrochloric acid**"; writing "nitric acid" or "methanol" gets wiped to `reagent`. Root cause: `extractReagents()` uses `name: r.keywords[0]` — **the first keyword in the group replaces the reagent name the user actually wrote**.<br>**The harm isn't a display glitch, it's identity rewriting on a path that touches the physical world**: the human reading `lab approve` reads the protocol's original text (sulfuric acid) but approves the hash of the compiled artifact (hydrochloric acid) — **what they approve isn't what they read**, which voids the point of AD-6's signed approval; the audit record ends up containing a chemical that doesn't exist anywhere in the protocol; the interception report is wrong as a result too.<br>Fixed: `name` now uses **the keyword that actually matched**, while `reagentId` remains the classification id (rule matching unaffected).<br>✅ (fixed before v0.5 release, back-tagged this round) |
| R2 | **`concentration_limit` renders "no rule found" as "✅ passed"** (🔴 fixed) | The second blocker from the same acceptance review. `MAX_CONCENTRATION[id] ?? Infinity` — **a reagent with no entry in the table gets an infinite threshold and is unconditionally allowed through**. The reviewer nailed the nature of it in one line: **"'I checked and there's no rule for this reagent' and 'I checked and it passed' are the same symbol in the output"** — a mirror-image violation of this project's red line "not found ≠ checked and it's not there," and it lands on the wet-lab safety gate.<br>Fixed, and **not a single threshold was invented**: no limit found → do not allow through, with the reason stated as "no rule to check against" rather than "over the limit" (the two call for different actions). Also added a check unrelated to the limit table: **a percentage > 100 is physically impossible and is always blocked**.<br>**Dug up something deeper in passing**: `extractConcentration()` used to **drop the unit** — both `%` and `mol/L` returned only a bare number — **the rule was comparing numbers without knowing their units**. The unit is now carried through (see V52).<br>This overturns one **deliberate** old assertion ("a reagent outside the table has no upper limit → allow, don't fabricate a standard out of thin air"): "don't fabricate a standard" is correct and still isn't done now — what's overturned is the "so allow it" part. |
| R3 | **`chem depict` doesn't work inside the binary, yet is claimed available in four places** (🟠 fixed) | Acceptance review measured `can't open file '/$bunfs/root/depict.py'`. **This is an omission from the V27 family** — W5-1 ε fixed 10 asset sites, but chem was a new file delivered in parallel by the same wave's γ, and neither lane covered the other's ground; **close-out (me) wired up `case "chem"` but never routed its `.py` through the embedding mechanism**. The consequence is worse than a typical ENOENT: `--help` / `capabilities` / MCP `tools/list` / `llms.txt` all four say it's available. Changed to use `materializeAssetTree`; measured on the real binary, it now produces an image.<br>✅ (fixed before v0.5 release, back-tagged this round) |
| V52 | Concentration-unit wording/policy not unified (what unit are `MAX_CONCENTRATION`'s numbers actually in?) | Dug up by R2: the parser now carries the unit through (`percent` / `molar` / `unknown`), but `MAX_CONCENTRATION`'s three numbers (hypochlorite 100 · ethanol 95 · strong_acid 200) **don't declare a unit** — strong_acid's 200 is meaningless in a percentage context. Currently only the unambiguous half is done (percentage > 100 is always blocked). **A real fix requires first defining this table's units, and possibly splitting the table by unit.** The reviewer also pointed out: a sodium-hypochlorite threshold of 100% means this rule only blocks physically impossible concentrations — commercial bleach is 5–15%. **This calls for domain judgment and shouldn't be decided unilaterally by the implementer.** |
| V53 | `doctor`'s `packagingLimitation` mechanism has zero coverage of real failures | Acceptance review measured: all three tiers of `doctor --json`'s `packagingLimitation` were **all false**, while `chem depict`, the only thing that actually produced a `/$bunfs/` error, **wasn't within doctor's probe scope at all**. INSTALL.md describes this detection mechanism (recognize the `/$bunfs/` substring → show ⚠️ rather than ❌) — **the field exists, the mechanism doesn't fire**. After R3 was fixed, this particular failure went away, but **the mechanism itself has still never been validated by any real failure**.<br>✅ **Patched in v0.6 W6-1 γ**: the core mechanism was already implemented back at F-c; γ added the two things the backlog literally called for — a `/$bunfs` mock-probe positive case + a bare-ENOENT negative control (must not be mistaken for a packaging limitation) |
| V54 | bioRxiv's "this isn't real search" disclaimer is invisible to humans | `capabilities --json`'s `caveat` states clearly "search is simulated from the most recent N papers plus client-side scoring, not found ≠ doesn't exist," but the two entry points humans actually use, `lit sources` and `lit search`, both show only ✅ / "no key required." The reviewer's comparison is stark: sources missing credentials are honestly labeled `⏭️ skipped` with a stated reason (one of this project's best-done aspects), **while bioRxiv uses a ✅ to paper over "this isn't search"** — honesty and dishonesty coexisting in the same output. The README has added an explanation (v0.5 close-out), but **the two CLI entry points still don't show the caveat**. Incidentally: `&amp;amp;` in titles isn't decoded and flows into BibTeX and reports.<br>✅ **Done in v0.6 W6-1 γ**: the caveat is now read directly from the connector metadata source of truth into both human entry points, `lit sources` / `lit search`; HTML entity decoding fixed at the same time (new helper in normalize.ts) |
| V55 | Wet-lab natural-language parsing only accepts Chinese | Acceptance review measured: an English protocol compiles to zero steps (errors outright, doesn't silently produce an empty protocol). **The reagent wordlist is bilingual, but the upstream step parser is not**, so the wordlist never even gets English input. The README has honestly added this note (the original text only said the wordlist covers Chinese and English, which reads as if the whole pipeline supports both).<br>✅ **Fixed in v0.8.0-alpha.2** (#100: step parser made bilingual, mixed Chinese/English can now be compiled) |
| V56 | Unknown commands don't echo back the mistyped word · reports don't carry unconsumedWarnings · observation tables get squashed onto one line | Three low-severity items from acceptance review: `spark-research frobnicate` prints a full screen of help but **never mentions `frobnicate`** (exit code is correct); `report export`'s wet-lab entries **don't carry unconsumedWarnings** — the README says the CLI's compile and approval output must display it, and the **report is a third surface that's currently a gap**; `report export` squashes an observation's markdown table onto one line.<br>✅ **Done in v0.6 W6-1 γ**: all three fixed (unknown-command echo / report export carries unconsumedWarnings / observation tables keep their formatting), regression test w61_cli_polish_output.test.ts |
| V57 | **A large swath of paths went unverified across all three external acceptance reviews** (not a defect, a coverage gap) | **A blind spot shared by all three zero-context acceptance reviews**, honestly recorded here so it isn't mistaken for "verified":<br>· **Every LLM path**: `idea new` / `idea check` / `lit read` / `lit review` / `chat` / `auth` — the reviewer **proactively did not run these**, per the discipline of "spending operations aren't auto-approved" (the environment did have real keys). README's Quick Start steps ②③⑥ therefore went entirely unverified.<br>· **`lab simulate` and a real Opentrons backend**: no opentrons on this machine, so the wet-lab chain was only verified up to `awaiting_approval`.<br>· **4 of the 5 simulation platforms** (openmm / scanpy / pydeseq2 / cobrapy): dependencies weren't installed on this machine, only pyref was verified.<br>· **Web workbench**: the frontend build wasn't built, so it was never started.<br>· **Modal compute**: the product's own documentation says the gateway isn't implemented, so it wasn't attempted.<br>· **The actual signed-approval writes for `conclusion review` / `lab approve`**: no reviewable conclusion card was produced, so this was never reached.<br>**Recommended disposition**: in the next version's acceptance-review task brief, list "spending paths" separately with a pre-agreed budget, otherwise it will **permanently** remain unverified — all three rounds skipped it for the same reason.<br>✅ **Cleared to zero in v0.6 R1** (spending paths pre-authorized for $2, ran the full LLM chain end to end; also covered by v0.7 R4/A6) — back-tagged this round |
| V58 | **The safety-gate semantics had just changed, and the third acceptance review ran before that change** | Before the v0.5 release, close-out changed `concentration_limit` from "allow reagents not in the table" to "do not allow," a **behavioral change** that could wrongly reject valid protocols. The change only went through unit tests and a few manual commands — **nobody walked through it again from a user's perspective**. A narrow-scope acceptance review has now been run (hitting only the safety gate, checking both false-rejection and false-pass directions).<br>**What's recorded here is a lesson, not a to-do**: **changing the thing an acceptance review covered, after that review, invalidates part of that review.** Either front-load the change before the review, or rerun the review afterward — you can't just assume "tests are green" means "verified." |
| V59 | Remaining gaps in the safety gate (found by narrow-scope acceptance review, **unfixed** before release, honestly listed) | The last narrow-scope acceptance review before release (hitting only the safety gate) dug up the following after the two blockers were fixed, **not fixed in v0.5**:<br>· **The limit table only covers 4 reagent classes** (hydrochloric/sulfuric acid · sodium hypochlorite · ethanol), and three of them have a threshold of exactly 100 — which completely overlaps with the "physically impossible" check, **so `100% sulfuric acid → ✅` only means "didn't exceed the physical limit," not "within the safety limit"**. An approver seeing this line would be misled.<br>· **`biosafety` only recognizes the literal strings `BSL-n` / "生物安全N级"**: "P3 lab" (the most common Chinese colloquial phrasing) silently passes with zero warning.<br>· **`chemical_compatibility` doesn't look at well positions**: sodium hypochlorite in A1 plus hydrochloric acid in B1 is also blocked, and the message doesn't say "this rule doesn't look at well positions."<br>· **The old `over-limit` message is substandard**: a fragmentary English sentence, doesn't state the limit, doesn't state the unit, gives no next step — a new branch within the same rule has a message an order of magnitude better in quality. `volume capacity` / `chemical compatibility` likewise give no next step.<br>· **The `lab compile` screen doesn't print a coverage-scope statement** (`lab status` does, and it's written well) — **and the overwhelming majority of people see the four lines of ✅ on the compile screen**.<br>✅ **The four software items fixed in v0.8.0-alpha.2** (#100: compile now prints a coverage statement (single source of truth SAFETY_COVERAGE_STATEMENT) · biosafety recognizes P-level · a note that well positions aren't checked · limit + unit + next-step for all three message categories; the hardware half is not done) |
| V60 | Identity is still lost for reagents outside the wordlist (only the collision was stopped) | v0.5 close-out made the placeholder unique per step (`unrecognized-reagent#step-1`), **stopping the real physical-aliasing bug where two different reagents shared the same reservoir well**, and added an unconsumed-warning. **But the user's original text wasn't preserved into the compiled artifact** — a human reviewing for approval still can't see that what they wrote was "nitric acid." A real fix requires the compiled artifact to carry the original text fragment, which is the parser's job.<br>✅ **v0.7 alpha.2 C-3 did the main body of this** (PR #66): the compiled artifact now carries `unrecognizedReagentText`, the step name becomes `unrecognized-reagent#step-1 (original text: nitric acid)`, and the CLI + frontend approval modal display the original text along with "outside the wordlist, not covered by safety rules," e2e ⑨c. **Residual**: `serialDilute`'s stock name, and out-of-wordlist reagents in `mergeReagents`'s sentence-continuation merging, don't yet carry the original text; original-text extraction is heuristic (if it can't be extracted, it isn't shown — no guessing) |
| V61 | **`existsSync("/$bunfs/…")` has platform-dependent semantics** (one instance fixed, registered for the shape) | v0.6 G-2's first Linux CI smoke test caught this: on macOS, calling it on the virtual path returns false (the embedded fallback kicks in), while on Linux it returns true but enumeration returns no content — so the Linux binary's skill index is **permanently 0 with no error**, exactly the kind of quiet wrong answer AD-12 most wants to avoid. `skills/frontmatter.ts` has been changed to use an explicit `/$bunfs` prefix check. **Registered for the shape**: any code that uses `existsSync` to decide whether to take the embedded fallback path could silently take the wrong branch on Linux; a grep pass confirmed `agents/prompts.ts` (try/catch fallback) and server/index's frontend path (a real directory after materialization) are unaffected, and there's currently no second instance. Any new fallback of this kind should always use the `/$bunfs` prefix check, never `existsSync` |
| V62 | `tests/e2e/tsconfig.json` has about 45 pre-existing type errors | W6-1 β honestly disclosed: Bun-specific imports (`.sql/.py/.md with` type attributes) error out under e2e's tsconfig; this predates the current round and isn't in any check set (`bun run typecheck` doesn't include the e2e tsconfig). Either fix the e2e tsconfig so it passes and fold it into typecheck, or explicitly document the exemption rationale. Assigned to v0.6's follow-up cleanup<br>📐 **v0.7 W7-D0 ruling: exempt, with the rationale documented** — all 45 errors are Bun-specific import attributes (`.sql/.py/.md with { type }`) erroring under e2e's plain tsconfig, unrelated to code correctness; actually running e2e is itself its type check. Not folded into `typecheck`; `tests/e2e/tsconfig.json` stays as-is. If a real type error in e2e ever gets masked by this noise, come back and write ambient declarations |
| V63 | Connector ledger's `rateLimitWaitMs` is permanently 0 | W6-1 α honestly disclosed: base.ts only sees the `HttpClient` interface and can't see how long `RateLimitedHttp`'s internal token bucket waited; a real record requires changing ratelimit.ts's return shape (outside α's ownership). The field currently exists but is permanently 0, honestly documented in a code comment. Wire it up if the B2 round observes rate-limit waiting becoming a bottleneck<br>✅ **Fixed in v0.8.0-alpha.2** (#96: ratelimit now returns the real wait in milliseconds, the ledger records it for real) |
| V64 | **The global `currentProject` pointer has no lock, so concurrent sessions cross-contaminate each other** (R1's top finding, confirmed both ways) | In B2 R1, two concurrent zero-context sessions each wrote papers into the other's project, and both abandoned and rebuilt their projects (R1/T1_report.md, T3_report.md). **A minimum defense line has been done (this round)**: all lit/idea/report commands get an explicit `--project <slug>` override + a help-text warning. **A real fix awaits design**: locking the pointer / session-scoped project binding (via env?) — touches every CLI entry point and state.json's semantics, filed as its own item<br>✅ **Really fixed in v0.7 alpha.5** (C-1, PR #74): a `state.json.lock` file lock + four-tier resolution + `project use` session binding. **Residual**: the CLI has no persistent session id, requiring the env var `SPARK_RESEARCH_SESSION`, otherwise it degrades back to the global pointer (with a warning) |
| V65 | AMiner search appears to do "approximate phrase matching" rather than keyword AND — **mechanism-level evidence for V8's extremely poor Chinese recall** | R1-T3 measured: compound queries (roughly >4 words / 6 characters) commonly get 0 hits, and Chinese terms are naturally compound words with no way around this; Chinese benchmark recall was 0/3. Fix direction: split search terms / merge multiple queries at the connector layer. **Evaluate a fix before R2**, otherwise Chinese-language rounds keep striking out entirely<br>✅ **Fixed before v0.6 R2 (mechanism fix + boundaries honestly stated)**: added a word-splitting fallback to searchOne for aminer — when the original query gets 0 hits and has multiple words, query word by word (deep pool ≥20), merge by hit-word count, and `status.note` honestly labels "this is a merged fallback result, not an original-query hit." Real API measurement: a query that R1 completely missed went from 0 → 8 relevant results; T3's frozen benchmark's top-20 single-word pool is 3/3 reachable. **Residual limitation (not concealed)**: a compound word written with no spaces (e.g. "motor-intent decoding" run together) still matches by word-sequence and gets 0 hits — with no Chinese tokenizer, it can't be split further; user-side workaround = put spaces between concepts (already noted in help text); post-merge top-10 ranking is still constrained by V67<br>**R2 verification**: T4's Chinese query 1 recall went to **3/3 (R1 was 0/3)**; query 2 triggered the fallback but the word-by-word results drifted off-topic (generalized perovskite results crowded out results specific to encapsulation/degradation) — the fallback's ceiling on relevance is bound by the same ranking issue as V67, honestly recorded<br>✅ **Residual done in v0.7 alpha.5** (B-2b): jieba added as an optional-dependency tokenizer, tokenizing first when a run-together compound word gets 0 hits; falls back to space-splitting and honestly labels this when jieba is missing |
| V66 | **Close-reading cards don't consume downloaded PDF full text (abstract-level reasoning only)** | R1-T1: all 10/10 cards showed abstract-level reasoning, including papers whose PDFs had been successfully downloaded. This is the biggest source of value in the literature pipeline — the "reading" in close-reading is currently false advertising. Need to check whether reading.ts's input construction connects to PDF text extraction at all<br>✅ **Done in the v0.6 R1 fix window**: pdf_text.py (pypdf) extraction + reading now injects full text (truncated at 40k characters) + basis/basisReason logged into record metadata + CLI displays "based on full text / abstract only." The fallback path never throws: no PDF / extraction failure / missing pypdf all fall back to the abstract with a stated reason. Real test: the AlphaFold3 paper's 24 pages yielded 40k characters extracted. pypdf is an optional dependency (missing it degrades gracefully overall); not yet added to doctor's probe surface — will be added if someone trips over it later |
| V67 | Default search ranking sinks milestone papers to the bottom | R1-T1: recall was 2/8 under the task-brief's search query, and only reached 5/8 once `--limit` was raised to 50; the 3 milestone papers (ESMFold/Foldseek/ProGen2) never appeared in search results at any setting, yet were found instantly by DOI — **a ranking problem, not a coverage problem**. Direction: weight by citation count / source-side ranking parameters<br>⚠️ **R2 strengthens the evidence**: T4's English task-brief query got recall 1/5@10, and raising the limit to 150 only got to 2/5, while the missing 3 papers were found instantly by DOI — **a pure ranking/merge problem** (including ranking after cross-source dedup). T2 was likewise 3/8 (highly-cited but off-topic papers crowding out foundational ones). Candidate main line for v0.7<br>⚠️ **v0.7 alpha.2 B-1 did the ranking part** (PR #69): `--rank blended|hits|citations|recent`, default blended, synced to the HTTP entry point (close-out). **Real verification conclusion: ranking is fixed, depth isn't enough** — under the default `--limit 10`, T1/T2/T4 recall@10 showed no difference from v0.6 (the milestones simply aren't in the shallow pool); at `--limit 50`, T1's RFdiffusion went from rank 20/28 → 6/5 into the top 10, and the other three papers went from 38/40/44 → 11/12/13. **Pending a user ruling**: whether blended mode should deepen the per-source fetch by default (perSource 30–50) in exchange for recall, at the cost of API call volume and rate-limit risk (the V26 token bucket is already in place). With no AMiner credential, T4's Chinese milestones are structurally unreachable<br>✅ **Deep pool done in v0.7 alpha.5** (B-2, PR #76): blended now defaults to 30 per source, overridable via `--per-source`; T1 recall@10 went 3/8 → 5/8. **T2/T4 not yet measured** (OpenAlex anonymous 429s); to be closed once R4 produces numbers<br>✅ **Closed in v0.8.0-alpha.2** (#99 α: measured with a polite pool, T1–T4 blended 8/24 · hits 7/24 · citations 7/24 · recent 0/24; the default tier and perSource=30 are kept as-is per the data; devlog W8-alpha) |
| V68 | `idea new` / `idea check` have no task handle | R1-T1: a killed process loses 100% of the work with zero trace — a capability imbalance with lit read/review (V35 already has a task handle). Wire it up the same way as runCliTask<br>✅ **Done in the v0.6 R1 fix window**: non-interactive idea new and idea check now go through runCliTask (kind=idea-new/idea-check), snapshotting to disk so status can be checked after being killed; on task failure, the original error reason is surfaced (not swallowed by wrapping, V36). Interactive mode is deliberately not wrapped (readline's lifecycle is under human control) |
| V69 | The project's `--desc` is injected verbatim into every LLM prompt, polluting report body text | Observed in both R1 topics: the desc text bleeds into close-reading cards' "relevance to this project" and reports' "research question." projectContext injection should be explicitly framed as background information, not task instructions<br>✅ **Done in v0.7 alpha.2 B-3** (PR #65): a single `projectBackgroundBlock()` helper in `agents/prompts.ts`, adopted at both real injection points — close-reading cards and co-explore; `review.ts`/`novelty.ts` verified to have no injection to begin with; `report/export.ts`'s desc is code-rendered, not a prompt, so it doesn't apply. 8 golden tests |
| V70 | After a long-task process is killed, the snapshot is stuck showing "running" | R1-T3: after a kill, `lit tasks` shows running forever, with no liveness criterion (a relative of V3: missing pid/start-time cross-verification)<br>✅ **Done in v0.7 alpha.2 C-2** (PR #67): the snapshot now records pid + start time, cross-verified on read-back → `orphaned` (a new state, not disguised as failed); three test categories for kill -9 / pid-reuse false positives / start-time unavailable degradation. Also fixed a real bug in passing where `ps lstart` without a timezone differed by 8 hours across processes. **Residual**: how the frontend task panel and MCP's TaskEnvelope display `orphaned` is only a suggested diff (devlog W7-C2), not wired up |
| V71 | `review findings` doesn't show a citation-integrity soft finding that was just written | R1-T1: a soft finding was just written to the evidence graph, but `review findings` can't see it — a hole in the query-filter surface or the type mapping<br>✅ **Done in v0.7 alpha.5** (B-4 reproduction + close-out wiring): root cause was that `lit review` never calls `findings().reviewTarget()` (no hole on the query side); close-out wired it up at the CLI's write point, reusing `reviewer/agent.ts`'s `computeFingerprint` for the fingerprint. The HTTP side, `routes/literature.ts`, has the same shape unwired, registered as a residual |
| V72 | BibTeX keys for authorless Chinese-language papers embed raw Chinese characters | R1-T3: the key should be converted to pinyin or degrade to anonymous+year; a key containing Chinese characters breaks some BibTeX toolchains<br>✅ **Done in v0.7 alpha.5** (B-4, PR #73): keys now only allow `[a-z0-9]`, with Chinese/authorless cases degrading to `anon<year>`; supersedes E-5 |
| V73 | Intermittent 401s from AMiner (12%, 12 out of 100 calls) | Captured by R1-T3's usage-api ledger; the key was within its validity period. To investigate: is this a rate-limit expressed as 401? Lack of permission on some endpoint category? Handle together with key renewal (10-07)<br>⚠️ **v0.6 re-verification: could not reproduce** — measured 0 401s across a 12-concurrent burst. R1's 12% at the time may have been transient server-side state or time-window-specific throttling; with no stable criterion, no rate-limit fix was fabricated (V26 discipline). Kept under observation; revisit if R2/R3 ledgers show it again<br>✅ **Disposed of in v0.8.0-alpha.2** (#99: retrospective ledger analysis found 401s strongly correlated with call intervals >30s → a single conservative retry added; this is a statistical correlation, not a controlled reproduction, honestly labeled as such) |
| V74 | **The test suite is not fully isolated from user-level config** | Found incidentally during R1: after a user ran `config set defaultModel`, sub_agent.test.ts's legacy assertion turned red (it was reading the real `~/.spark-research`). That test has been fixed (env isolation); **a systemic fix is still pending discussion**: uniformly injecting a temporary `SPARK_RESEARCH_DATA_DIR` via bunfig preload — but first need to inventory which tests deliberately read real credentials (RECORDING mode)<br>✅ **Systemic fix done in v0.7 alpha.6**: `bunfig.toml` preloads `tests/preload.ts`, which injects a temporary `SPARK_RESEARCH_DATA_DIR` into every bun test process, throwing immediately if anything lands under ~/.spark-research; e2e's webServer is isolated the same way; gate `test_isolation.test.ts`. Recording/live modes are exempt |
| V76 | **154 skills / 29 connectors staged during v0.5 planning have never been registered in the repo** (an inventory that lives outside the repo, under no version control) | **Back-registered 2026-09-11**. During v0.5 planning, a subagent did a full inventory of OpenScience's 313 skills / 46 connectors and staged migratable code; the source of truth lives locally at `~/Desktop/AI4S/spark-research-v0.5-plan/workstreams/` (70MB, **not a git repo, no backup of any kind**): `skills/staged/` has **154** SKILL.md files (inventory tiers P0=14 / P1=67 / P2=89 / SKIP=143) · `connectors/staged/` has **29** connectors, each with its own `.test.ts`.<br>**Actually landed: 3/154 and 4/29**: only cobrapy / pydeseq2 / scanpy made it in as skills (W5-3 β), and only biorxiv / clinvar / reactome / string-db made it in as connectors (W5-2 γ). The 25 connectors not landed: arrayexpress · bindingdb · biogrid · chebi · dbsnp · depmap · expression-atlas · geo · gnomad · gtex · gtopdb · hpa · intact · interpro · kegg · mygene · myvariant · ncbi-gene · opentargets · pdbe · sifts · single-cell-atlas · surechembl · ucsc · wikipathways.<br>**What's being registered is the inventory, not a scale-up to-do list.** The policy hasn't changed: AD-5's "few and deep — every skill must have e2e verification to count as done," `DEVELOPMENT_PLAN_v0.3.md` explicitly lists "chasing OpenScience's 313-skill / 46-connector count" as out of scope, and the v0.6 plan's §Explicitly Not Doing likewise lists "scaling up skills/connectors (only integrate what B2 names, ≤2–3 per round)." This item exists because **the repo previously had no pointer to any of this** — the next session could either assume it doesn't exist, or wrongly assume it's already in the repo.<br>**Two things that must be known before migrating**: ① every staged file is marked `STAGED / UNTESTED` — not wired into the registry, never run against real network traffic, no fixtures recorded; `REGISTRY_PATCH.md` opens by stating "none of the operations this document describes have been carried out"; ② `BATCH2_B.md` records a **common bug spanning more than a dozen staged test files**: when `echoHttp` responds with `content-type: application/json`, `connector.call()` already receives a parsed object, so the boilerplate's `JSON.parse(raw as string)` always throws `SyntaxError` — grep for this shape before migrating in any batch.<br>**Disposition**: per v0.6's policy, driven by real research tasks in B2 rounds, integrate ≤2–3 selectively per round, each going through fixture recording + the full six-suite check; **no quantity target is set**. Whether the planning directory itself should be checked in (or turned into a separate private repo) is a separate discussion — sitting bare on the Desktop is currently the only copy. |
| V77 | OpenRouter occasionally doesn't return usage statistics (1 out of 151 calls), that call's cost is logged as unknown | Measured from R3-T1's ledger. **This is the "unknown is not treated as zero" discipline working as intended**, not a pricing-table gap. If the unknown rate rises, consider tokenizer-based estimation with an explicit "estimated" label; the current occurrence rate doesn't warrant it |
| V78 | LLM calls on the orchestrator/chat path never enter usage.jsonl | The A5 fix (alpha.7) wired metering into the four HTTP routes read/review/idea/novelty; chat (OrchestratorAgent, which builds its own LLMRouter) and the MCP surface are still unwired. chat has P13's frame-level accounting into the evidence graph but it doesn't enter the usage ledger — inconsistent wording, to be unified next round<br>⚠️ **v0.7 W7-D0 fixed the main body**: the chat path (OrchestratorAgent) now enters usage.jsonl and raw/llm (command=chat) via `llmFor(sessionId)`; the MCP surface already goes through the Hono app's HTTP routes and has been metered since alpha.7 (this was previously mis-registered). **Residual**: `subAgentLlm()` (the subagent tool loop) still calls raw — it has no sessionId to bind to a project; subagent frames are accounted for in agent_run but don't enter usage.jsonl/raw. Pass a sessionId to runSubAgent after W7-D1 if needed<br>✅ **Residual fixed in v0.8.0-alpha.2** (#96: `subAgentLlm(sessionId)` now goes through usageTrackingLlm, command chat:subagent) |
| V79 | Three low-severity UI items left over from A5 | ① a citation span in a review looks clickable but doesn't actually jump to the evidence graph ② the prerequisite that `conclusion review` requires an existing experiment isn't documented in the UI ③ UI spending operations have no budget-parameter entry point (the CLI has --budget-usd). All registered as non-blocking for release<br>✅ **①②③ fixed in v0.8.0-alpha.2** (#97; ③: chat-style co-explore goes through orchestrator's own `llmFor`, which isn't wired to the budget → V119) |
| V80 | Concurrent write operations within the same project hit SQLite's `database is locked` | Measured by R3-T4 (idea new and idea check running concurrently); serial retry succeeded. Direction: busy_timeout or a write queue; a low-frequency case in the single-user scenario<br>✅ **Done in v0.7 W7-D0 (C-4)**: added `PRAGMA busy_timeout = 5000` to the three databases records / library / artifacts (findings.db already had it). Adding a concurrency-reproduction script to the concurrency suite is left to be done together with C-1<br>✅ **Root cause really fixed in v0.7 alpha.6**: C-4's busy_timeout was only a necessary condition. ① write transactions changed to `.immediate()` (a DEFERRED read→write upgrade gets an immediate BUSY); ② each database's table creation + migrations moved into an IMMEDIATE transaction (a race on the first concurrent ALTER). Two-process real reproduction tests `records_write_race` / `project_write_race` added to the concurrency suite; negative control: reverting to DEFERRED → locked 7 times |
| V81 | **v0.6.0 shipped with an e2e test that's guaranteed to fail when the full suite runs** (⑱, the usage panel; green alone, red in sequence) | Found when v0.7's baseline gate 0-1 actually ran the full six-suite: after alpha.7 (the A5 blocker fix) wired the HTTP routes read/review/co-explore/novelty into `llmFor` metering, e2e ③④⑤ write rows into this project's usage.jsonl, so ⑱'s opening assertion that "the panel must start empty" only holds when run in isolation. CI only runs unit tests + smoke tests, not e2e, so nobody saw this. **Same shape as V58**: something the acceptance review covered got changed afterward (HTTP metering was added after A5), and the full e2e suite was never rerun.<br>✅ **Fixed in v0.7's baseline gate**: ⑱ changed to an incremental assertion (before/after deltas of calls/unknownCostCalls/knownCostUsd are +2/+1/+0.0123 respectively), checking the empty state only when there really are no records. **Registered for the shape**: e2e tests share one project directory, so any assertion that "assumes some file is empty" is order-sensitive; all new e2e tests should write incremental assertions. Also: the gap of CI not including e2e was carried into the W7-D0 close-out agenda (either add it to CI, or have the release checklist explicitly list "run the full local e2e suite") |

| V82 | **`execution_records` was built but has no production writer** (the 5th instance of the V45 family) | Found while wiring up raw/kernel during v0.7 W7-D0: `artifacts.db`'s `execution_records` table and `ArtifactStore.saveExecution()` are referenced only by tests; kernel cell execution (`KernelManager.execute()`, whose only production caller is orchestrator's code task) has never written to this table — `report stats`'s related statistics are permanently empty for it. The orphan gate can't catch this because `artifacts/store.ts` as a whole has production callers (the file-granularity ceiling noted back in W3-c, same shape as V30). **D0 disposition**: not wired up opportunistically (outside the footprint, and wiring it up would require deciding frame/cell_index semantics); raw/kernel rows keep `executionRecordId` permanently null and carry their own source/stdout/stderr + contentHash, not depending on it. Disposition: decide together with the journal at W7-D1 — either kernel execution really writes to execution_records and raw rows reference it, or delete the table<br>⏸ **Not resolved at W7-D1** (wiring it up requires deciding frame/cell_index semantics and touches orchestrator). **Disposition: decide together with export at W7-D2** — export will honestly export execution_records as an empty table<br>✅ **Wired up in v0.7 W7-D2**: orchestrator's code task now writes to `execution_records` on every kernel execution (frame=sessionId, cell_index incrementing); `data export`'s `artifacts/execution_records.jsonl` is no longer permanently empty. **Residual**: raw/kernel rows' `executionRecordId` is still null (raw is written first inside execute()); the two sides align via contentHash |
| V83 | **Unit tests wrote 324MB of raw data into the user's real `~/.spark-research`** (an amplified version of V74, during alpha.1–alpha.5) | Found in R4's evidence: `~/.spark-research/raw/connector/` had 43,680 lines (including scaffolding's fake connectors `openfree`/`paidsource`), `raw/kernel/` had 253 lines, and `api_calls.jsonl` had 59,462 lines (accumulated since W6-1). Root cause = the raw layer's global fallback sink computes `dataDir()` on the fly + unit tests don't isolate the data directory. **The code side has been fixed** (V74's systemic fix). **The pollution already sitting in the real directory hasn't been touched** — R4's real-task connector rows are mixed into the same files (can't be reliably separated by host/time); whether to archive (`raw/` → `raw.polluted-2026-09-11/`, reversible) or delete is left to the user's ruling<br>✅ **Archiving done (user ruling, 2026-09-11)**: `~/.spark-research/raw/` → `raw.polluted-2026-09-11/` (323MB), `api_calls.jsonl` → `api_calls.polluted-2026-09-11.jsonl`, a reversible mv; raw data inside project directories is unaffected |
| V84 | `--for-sharing` leaks upstream summaries via raw/llm's prompt (R4 P0-1) | ✅ **Fixed in alpha.6**: under for-sharing, llm rows' `messages` now store only a hash, counted in `manifest.excluded.llmPromptsHashed`; the import side only verifies the chain for rows of this kind. **Registered for the shape**: "a row being model_generated doesn't mean its input is also model_generated" — the classification looks at the output; the input needs to be filtered separately |
| V85 | The raw layer's "four kinds" don't include simulation-platform execution | R4's T2 ran `exp run --platform scanpy` intending to verify kernel raw, and found that `exp run` goes through a SimulationPlatform child process, not KernelManager, so it isn't in raw at all — only the artifact and the observation (with contentHash) are. Whether to also log simulation prepare/submit/collect into raw (kind=simulation) is pending discussion — the artifact is already an append-shaped record, so the added value is uncertain<br>✅ **Fixed in v0.8.0-alpha.2** (#96 β: raw `kind=simulation` added, export/import/verify synced) |
| V86 | OpenAlex's anonymous pool gets 100% 429s under deep pool × concurrency, recall@10 is 0/8 across the board (R4 P1-7) | ✅ **alpha.6 added host-level rate limiting for `api.openalex.org`** (official 10 rps). **Recall not yet verified**: waiting for the 429s to subside before A6 retests. User-side workaround: `config set contactEmail <email>` to enter the polite pool (see ratelimit.ts for the original policy text). V67's "at least +2 per topic" has still not been measured on T2/T4 as of this point<br>✅ **Closed in v0.8.0-alpha.2** (#99: 38 real calls to OpenAlex, 0 429s) |
| V87 | `lit review`'s gap between "citations parsed" and "citations judged" is unexplained (R4 P2-10) | All four topics show a gap (T1 76/64 … T4 113/98), and neither the CLI nor the observation record explains where they went (dedup? self-citation? silently skipped on parse failure?). Check `citation_judge`'s counting logic and explain it in the output<br>✅ **Fixed in v0.8.0-alpha.2** (#99: `explainCitationGap()` gives an exhaustive breakdown of dedup/self-citation/parse-failure/other, synced between CLI and observation) |

| V88 | The close-reading task's progress bar isn't real-time (A6 Medium C-1; A5 also recorded 0/9) | `lit read --all` ran for 7.8 minutes with the task panel's `progress.done` at 0 the entire time, jumping to 10 the instant it finished. Data is correct, purely a UX issue. Check whether `runCliTask`'s progress callback is being swallowed by batching<br>✅ **Fixed in v0.8.0-alpha.2** (#97 γ: reports back after each paper, e2e asserts intermediate states) |
| V89 | A single co-explore message produces 2 highly similar Idea cards (A6 Low) | Unclear whether this is deliberate (primary/backup hypothesis) or the same generation being logged twice. If deliberate, add a note to the UI; if not, dedupe<br>✅ **Fixed in v0.8.0-alpha.2** (#97: ruled a duplicate record, deduped within the same sessionId by normalized hypothesis) |
| V90 | The project dropdown shows only the name, not the slug, so an imported artifact with the same name as the original project can't be distinguished (A6 Low) | `data import` carries `dcat.title` over as-is. Either show the slug in the dropdown or add a suffix to imported artifacts<br>✅ **Fixed in v0.8.0-alpha.2** (#97: dropdown now shows "name (slug)") |
| V91 | **The raw chain breaks under multi-process append** (the real cause of A6's `verified:false`) | ✅ **Fixed in alpha.7**: `JsonlRawSink.append` now re-reads the previous line's hash from the file tail on every call, inside a `<file>.lock` critical section; `importEntry` does the same; import no longer reorders by ts. Two processes doing 200 appends produced an intact chain (concurrency suite). **Registered for the shape**: "caching file state per process" is wrong on any path that allows both a server and a CLI to be used concurrently — C-1's state.json uses a file lock, and so does this one; the pattern is now unified. Historical breaks are not backfilled |

| V92 | **Chinese-character citation keys are completely invisible to both verification gates** (external review P1-1, reproduced empirically by the main session) | `reviewer/rules.ts:96`'s CITATION_TOKEN character set doesn't include Han characters — measured `citedKeys("[@李某2023神经解码]")` returns empty. Bad in both directions: a forged Chinese key bypasses verification and the judge; a draft with purely Chinese keys is falsely reported as "no citations." **V72 (#73) already keeps newly generated keys from embedding Han characters, so the main generation path is already cut off**; the residual is forged/hand-written/existing-library scenarios. Fix: add `\p{Script=Han}` to the character set (along with the strong assertion check at :296), add an adversarial test "a forged Chinese key → hard"<br>✅ **Fixed in v0.8.0-alpha.2** (#95 δ: CITATION_TOKEN now includes Han characters, negative control confirms an out-of-library Chinese key is always caught) |
| V93 | **Budget-gate TOCTOU: concurrent in-flight calls can collectively bypass the gate** (review P1-2, confirmed at the code level; merged into P2-6/P2-9) | The gate check and the accounting are separated by the entirety of `await llm.call` — under `Promise.all`, N in-flight calls all pass the gate before any of them are accounted for; across processes, `priorKnownCostUsd` is only read once, at construction time. Also: no pre-flight cost estimate (a single call can spend 158% of the budget, confirmed by the project's own tests); concurrent `lit read --all` with no project lock can double-spend. Fix: the gate check re-reads totals in real time + an in-process reservation-and-settlement scheme + a project lock for read; at minimum, document the upper bound of concurrent overrun<br>✅ **Fixed in v0.8.0-alpha.1** (#86, devlog G8-3: in-flight reservation + real-time re-read + pre-flight cost estimate; `lit read`'s project lock left as V85) |
| V94 | **An unpriced model makes the budget gate silently ineffective** (review P1-3, confirmed: the anthropic price table has zero entries) | priceFor returns null → everything goes into unknown → knownCostUsd is permanently 0 → the gate never fires, so a user who sets --budget-usd gets no actual protection and no warning. Fix: look up the default model's price when the wrapper is constructed, and emit an stderr warning "the budget gate is ineffective for this model" if it's null; `SPARK_LLM_PRICING_JSON` override already exists, add rejection of negative values<br>✅ **Fixed in v0.8.0-alpha.1** (#87, devlog G8-4: unpriced models are rejected by default + `--allow-unpriced` + five anthropic entries added) |
| V95 | **The HTTP surface for lab approve/simulate constitutes an approval bypass** (review P1-5; a concrete instance of V10/V19, **a hard prerequisite for connecting real devices**) | `POST /api/lab/experiments/:id/approve` approves as long as actor is a non-empty string (self-reported, no verification), and simulate then executes — any local process can curl its way through design→approve→execute, completely bypassing the CLI's TTY gate. compute has already pulled dispatch out of HTTP (AD-14 defense-in-depth), lab hasn't been aligned to match. Fix: either pull it out of HTTP (pop up "go run this from a terminal") or require a one-time confirmation token at server startup<br>✅ **Software half fixed in v0.8.0-alpha.2** (#100 ε: HTTP approve/simulate now require actor + a one-time token (`lab token <id>` issued behind the TTY gate, 10-minute validity, single use); 403 without a token; devices are not wired up) |
| V96 | state.json writes are non-atomic (the half of review P0-1 that holds up) | The lock already exists (enhanced by #74), but `writeState` is still a bare writeFileSync — a process crashing mid-write can leave a truncated JSON file (state silently zeroed out). Fix: atomic write via temp+rename; change `create()`'s exists→mkdir to a `recursive:false` pre-emptive check. **The part of the review that was inaccurate is honestly noted: its claim of "no lock" does not hold — state.json.lock (O_EXCL) already existed in the reviewed version**<br>✅ **Fixed in v0.8.0-alpha.1** (#88, devlog G8-5: tmp+fsync+rename; the `create()` pre-emption half is untouched) |
| V97 | The usage ledger's `model` field has no validation and can be written as an object, rendering as `[object Object]` (review N1, reproduced empirically in an e2e project) | Root-cause chain: the test ScriptedLlm doesn't normalize CallOptions → res.model ends up as an object → ledger append writes to disk without validation → the frontend's per-model aggregation table renders incorrectly. Fix: before writing to disk, check `typeof model === "string"`, otherwise record "(unknown)" and count it as corrupt; added normalization to ScriptedLlm<br>✅ **Fixed in v0.8.0-alpha.2** (#96: type guard on append + ScriptedLlm normalization) |
| V98 | **`basis/basisReason` is lost on read-back — "based on full text / abstract only" only lives at the moment of generation** (review P2-3, confirmed cardFromRecord has zero read-back) | The review prompt and the judge can't get the material-level flag, weakening V66's auditable claim (the metadata is in the record, but the consumer never reads it). Fix: restore the two fields to cardFromRecord; label each card's material level by basis in the review prompt<br>✅ **Fixed in v0.8.0-alpha.2** (#96: cardFromRecord read-back added + both the judge's input and the review-draft prompt are labeled by basis) |
| V99 | Three wording/policy debts in the usage ledger (review P2-7/P2-8/P2-10) | ① a ledger write failure throws away already-paid-for output, while api_ledger explicitly states "a write failure must never affect business logic" — two different disciplines in the same repo; ② auth/rate_limit failures (provably \$0) are also recorded as costUsd=null, polluting the "unknown" semantics and making the total permanently unreportable with confidence; ③ embedding calls are entirely outside the ledger (novelty is under-reported and the gate can't manage it). Fix: swallow + warn under the same discipline; distinguish known-\$0 from truly-unknown by error.kind; either bring embedding into the ledger or document it as a separate, itemized case<br>✅ **Fixed in v0.8.0-alpha.2** (#96 ①②; ③ embedding brought into the ledger by #94 ζ) |
| V100 | compute uploads' `workspaceRoot` accepts an arbitrary absolute path (review P2-1, security) | An unauthenticated local call can drive a full read + sha256 of any directory tree (the quota check happens after hashing), plus directory enumeration. Fix: after resolving, force it to land inside the project directory; move the quota check into walk()<br>✅ **Fixed in v0.8.0-alpha.1** (#84, devlog G8-1) |
| V101 | extensions' `--trust` fingerprint only covers the single entry file (review P2-2, security) | Modifying a helper file can bypass TOFU. Fix: fingerprint the closure of static imports, or a directory-manifest hash<br>✅ **Fixed in v0.8.0-alpha.1** (#85, devlog G8-2: directory-manifest fingerprint) |
| V102 | Miscellaneous items bundled from the external review (P2/P3 backlog, to be split item-by-item or explicitly declined in v0.7 review) | · P2-5: HTTP-side review doesn't log a citation-integrity record (CLI/HTTP semantics diverge) · P2-11: a second instance crashes outright on the fixed 4321 port · P2-13: six god files (orchestrator at 1300+ lines, etc.) · P2-14: inconsistent error envelope · N3: chem produces 10 fails instead of skips when rdkit is missing (inconsistent with scanpy/pydeseq2's convention) · N4: pyproject.toml's version, 0.2.0, doesn't track the release version (the version-sync gate should be extended to cover it) · P3: a single-page exception in pdf_text takes down the whole document (needs a per-page try) · the 40k full-text truncation is unaware of model token limits and still labels itself fulltext after truncating · the word-splitting fallback has no negative cache (an agent loop could amplify quota consumption) and still reports ok even when every word fails · rebuildCitations is O(N·R·C) · narrative-style citations are outside the verification's field of view (the prompt doesn't mandate the `[@key]` form) · auth's interactive input doesn't disable echo · the CI bypass token goes through argv + a non-constant-time comparison · HTTP and CLI batch close-reading have inconsistent skip semantics. Source of truth: docs/reviews/v0.6.0_external_review.md<br>✅ **Split item-by-item into V104–V117 in v0.8.0-alpha.2** (this item closed; see each item's own row for its disposition) |
| V103 | The repo lacks a reproducible test for "zero cross-contamination under concurrency" (a suggested regression-defense line from the review) | "Four sessions, 22 minutes, zero cross-contamination" is currently only anecdotal test evidence. Fix: a real concurrency e2e test — 4 child processes running mixed, some with `--project` and some without, asserting records never cross-write. Once #74's session binding lands, this test also guards it <br>✅ **Done in v0.7 C-1**: `tests/concurrency/project_pointer.test.ts` runs two real child processes, each doing `project use` + a record write 100 times alternately, with zero project cross-contamination; R4's four-topic concurrency run measured zero project cross-contamination |
| V104 | HTTP review doesn't log a citation-integrity record (V102 split · P2-5) | CLI `lit review` logs a record, HTTP `POST /api/lit/review` doesn't → the same capability with two different semantics. Disposition: **v0.8 R5 fix-window candidate** (just needs the route to pass a flag through)<br>✅ **Fixed in the v0.8 R5 window** (HTTP review now logs a citation-integrity observation record, response includes citationReviewRecordId/citationGap) |
| V105 | A second instance crashes outright on the fixed 4321 port (V102 split · P2-11) | `server` throws a raw EADDRINUSE stack trace when the port is taken. Disposition: v0.9 (change to a readable error + suggest `server <port>`) |
| V106 | Six god files (V102 split · P2-13) | orchestrator.ts at 1300+ lines, etc. Disposition: **registered as won't-do** (in v0.9, split naturally by file ownership as lanes need it; no pure-refactor PR) |
| V107 | Inconsistent error envelope (V102 split · P2-14) | `{error}` / `{error,detail}` / plain text are mixed together. Disposition: v0.9 (the contract already pins `ApiErrorBody` as a schema; use it as the reference when unifying) |
| V108 | chem produces 10 fails instead of skips when rdkit is missing (V102 split · N3) | Inconsistent with scanpy/pydeseq2's convention. Disposition: v0.9 |
| V109 | pyproject.toml's version doesn't track the release version (V102 split · N4) | The SDK's `sdk/python/pyproject.toml` already has a version-sync gate (#98), but the repo root's `pyproject.toml` is still 0.2.0. Disposition: **done during v0.8 close-out** (the same gate extended to cover it) |
| V110 | A single-page exception in pdf_text takes down the whole document (V102 split · P3) | Needs a per-page try. Disposition: v0.9 |
| V111 | The 40k full-text truncation is unaware of model token limits and still labels itself fulltext after truncating (V102 split · P3) | Disposition: v0.9 (basis should have a third tier, `truncated`) |
| V112 | The word-splitting fallback has no negative cache, and still reports ok even when every word fails (V102 split · P3) | An agent loop could amplify quota consumption. Disposition: v0.9 |
| V113 | rebuildCitations is O(N·R·C) (V102 split · P3) | Disposition: v0.9 (not yet a bottleneck at current scale) |
| V114 | Narrative-style citations are outside the verification's field of view (V102 split · P3) | The prompt doesn't mandate the `[@key]` form. Disposition: v0.9 (re-review together with the V13 ruling) |
| V115 | auth's interactive input doesn't disable echo (V102 split · P3, security) | Disposition: **v0.8 R5 fix-window candidate** (disable echo on readline, a few lines)<br>✅ **Fixed in the v0.8 R5 window** (`auth`'s key entry now goes through readHidden: raw TTY with no echo, line-based over a pipe) |
| V116 | The CI bypass token goes through argv + a non-constant-time comparison (V102 split · P3, security) | Disposition: **v0.8 R5 fix-window candidate** (`timingSafeEqual` + allow passing via env)<br>✅ **Fixed in the v0.8 R5 window** (sha256+timingSafeEqual constant-time comparison; `--ci-bypass-token-env <VAR>` avoids argv) |
| V117 | HTTP and CLI batch close-reading have inconsistent skip semantics (V102 split · P3) | Disposition: v0.9 |
| V118 | openmm's probe is still an inline string (V51 residual) | runner.py has no `probe()` entry point; switching directly to `probeCodeFor` would make the whole P5 contract test suite skip (measured during #95 close-out). Disposition: v0.9 (add probe() on the Python side)<br>✅ **Fixed in the v0.8 R5 window** (openmm's runner.py got a probe(), the TS side now uses probeCodeFor; the P5 contract test no longer skips) |
| V119 | Chat-style co-explore has no budget entry point (V79③ residual) | Goes through `agents/orchestrator.ts`'s own `llmFor`, which the UI's budget parameter can't reach the gate through. Disposition: **v0.8 R5 fix-window candidate** (add budgetUsd/allowUnpriced to orchestrator's llmFor)<br>✅ **Fixed in the v0.8 R5 window** (the chat/stream route now passes budgetUsd/allowUnpriced through → orchestrator tracks the budget per session; the UI chat box got a budget entry field; the gate's rejection message is passed back to the user verbatim) |
| V120 | `records_write_race` occasionally hits database is locked (observed during alpha.2 close-out) | While rerunning #99, two processes doing idea-check-style writes hit a locked error once in 60 tries (failed within 382ms, not a timeout); reran in isolation 3/3 green; machine load 4–11. Disposition: **check before v0.8 A7** (whether busy_timeout fails to cover some path) |
| V121 | **The raw chain silently breaks when a single line exceeds 64KB** (R5 P1-4's real cause, empirically confirmed by the main session) | `readTail` uses a fixed 64KB window and doesn't distinguish "a complete line" from "a partial line": a connector's response body is inlined as one whole line, and when that line is 68–70KB, the window lands entirely inside it; parsing the partial line with `JSON.parse` throws, which gets swallowed by a catch into `last=null`, so the next entry writes `prevHash:null` → the chain breaks. The llm chain is immune (`body()` moves anything >64KB into blobs). **Empirical confirmation**: in all four R5 projects, every one of the 4 broken-chain points had a preceding line of exactly 68444/68775/70330/68398 bytes, without exception; the break happens **at write time**, so `data import`'s report is honest.<br>✅ **Fixed in the v0.8 R5 window**: the window now grows as needed (64KB→1MB→16MB→whole file) + a partial line is never parsed; if the last line still can't be parsed, it throws outright rather than fabricating a chain head. **Historical breaks are not backfilled** (backfilling would mean fabricating data) |
| V122 | **The HTTP/SDK `body.project` field is silently ignored, and writes go to whatever project the current pointer points to** (R5 P0-2, reproduced by the main session) | `projectSlug()` only reads from the query string. The SDK generates body-style requests, so "explicitly specifying a project still writes to the wrong place," and the response body even reports back the wrong project name (R5 measured 3 papers written into an unrelated project, a5-accept, which have since been tombstoned and rolled back).<br>✅ **Fixed in the v0.8 R5 window**: `jsonBody()` now attaches the body to the context, and `projectSlug()` resolves in the order query > body > current pointer, fixed once and covering all 47 write-route call sites |
| V123 | Every SDK POST with no body gets a 415 + `docs/SDK.md`'s return-type assertion is too broad (R5 P1-6 / P1-5) | Generated methods default to `body=None`, and the old `request()` only sets `Content-Type: application/json` when there's a body, while the server enforces that header for write requests → `lit_post_lit_papers_by_id_pdf` and others fail 100% of the time, forcing a fallback to the CLI. Also, `docs/SDK.md` claims `tasks_get_tasks_by_id` returns a top-level `TaskResponse`, but it actually returns `{"task":{...}}`.<br>✅ **Fixed in the v0.8 R5 window**: write methods with an empty body now send `{}` with the JSON header; the documentation's assertion that didn't match measured behavior was removed |
| V124 | R5 P0-3, "export/import round-trip produces a non-empty report diff," **does not hold up on re-verification** (a methodological artifact, not a defect) | Main-session verification: after export→import of the same project, running `report export` on both sides with `--verbose` → diff is **0 lines** (timestamps only); the shape R5 described (Idea/experiment-card body text disappearing entirely) only appears when "the original report used --verbose and the copy's did not" — what's "missing" is exactly the `record.content` that only renders under verbose. Recorded here to prevent re-registration. See `docs/devlog/R5-window-fixes.md` |
| V125 | **The concurrent budget gate can be bypassed across requests** (A7 Blocker-1, reproduced by the main session) | V93's fix addressed "N concurrent calls within one ledger"; on the HTTP surface, each request constructs its own `usageTrackingLlm` → its own ledger, so in-flight reservations aren't visible to each other, and the other half of the gate (real-time re-reading of usage.jsonl) can only see **already-settled** spending → N concurrent requests all pass the gate at once. A7 measured 10 concurrent requests × `budgetUsd=0.03` actually spending $0.0807 (2.7×).<br>✅ **Fixed in the v0.8 A7 window**: in-flight amounts are now aggregated by usage.jsonl path into a **process-level** registry, so concurrent requests within the same process can see each other. **Residual (honestly noted)**: in-flight amounts still aren't shared across processes, so the upper bound is one call's estimated cost per process |
| V126 | **The HTTP request body's `model` field is silently ignored** (A7 Blocker-2) | `routes/literature.ts`/`ideation.ts` always use `ctx.model()` and never read body.model; and `configuredDefaultModel()` takes no root, so it doesn't read the config from this server's own data directory. The same string works fine via the CLI's `--model`.<br>✅ **Fixed in the v0.8 A7 window**: `ctx.model(override?)`, with priority body > deps.model > this server's root config |
| V127 | **V21's "errors immediately on startup" promise doesn't hold for doctor** (A7 High-3) | `resolveSetting` only throws when that setting is **actually read**; `doctor` never touches the timeout settings → running under any of the four old names silently exits 0, contradicting the promise in `docs/INSTALL.md`.<br>✅ **Fixed in the v0.8 A7 window**: `legacyEnvViolations()` scans for all removed old names during `main()`'s startup, exiting 1 on any hit (measured: `SPARK_HTTP_TIMEOUT_MS=1000 doctor` → rc=1) |
| V128 | **Subcommand-level `--help` actually runs a command with side effects** (A7 Medium, caused a real side effect during acceptance review) | V39 added subcommand-level help to lit, but `data`/`auth` were missed: `data export --help` directly runs a real export (A7 caused a real export against the unrelated project r5-t2 because of this, already cleaned up), `auth --help` goes straight into interactive entry.<br>✅ **Fixed in the v0.8 A7 window**: any `data` subcommand seeing `--help/-h` now prints help and returns first; same for `auth --help` (new AUTH_HELP). **Residual**: the rest of the modules haven't been individually checked, left as V131 |
| V129 | A7 High-1, "`/api/tasks/:id` returns invalid JSON," **does not hold up on re-verification** | Main-session verification: `JSON.stringify` correctly escapes newlines/tabs; starting a real server, creating a record with newlines in its body text, then `GET /api/records`, parses through Python's strict `json.load` and the body text round-trips intact. A7's jq error was caused by its own sampling/piping method, not a product defect. Registered to prevent re-registration |
| V130 | `/api/usage` without a project returns the current project instead of a global summary; the CLI exit code is still 0 when an unpriced model is rejected; `config get`'s source attribution labels config.json values as env (three Medium/Low items from A7) | All reproducible but non-blocking for release. Disposition: v0.9 |
| V131 | The remaining modules' subcommand-level `--help` hasn't been individually checked (V128 residual) | `data`/`auth` are fixed; `exp`/`lab`/`compute`/`protein`/`chem`/`report`/`conclusion`/`review`/`ext`/`new` have not each been verified for "subcommand --help has no side effects." Disposition: v0.9 (or add a structural gate: every runXCommand must return immediately on seeing --help) |
| V132 | **The contract fails to declare `budgetUsd`/`allowUnpriced`** (A7 High-2, A7's judgment was correct) | The route accepts these two fields (V119), but `server/types.ts`'s `ChatRequest` doesn't declare them; the contract's HTTP schema is generated from the TS interface → neither `contract --json` nor the SDK can see them, so the contract lies by omission.<br>✅ **Fixed in the v0.8 A7 window**: `ChatRequest` got the two fields added and the schemas were regenerated. **Residual**: the contract gate only cross-checks the set of routes, not "which body fields a route actually reads" → V133 |
| V133 | The contract gate doesn't cross-check "the body fields a route actually reads" (V132 residual) | If someone adds a route parameter and forgets to sync `server/types.ts`, the existing gate won't notice. Fix: extract `optionalString(body,"x")`/`requireString(body,"x")` calls from route source code and cross-check them against the corresponding request type's property set. Disposition: v0.9 |

> **2026-09-13 batch (English from here on, per project-wide documentation-language decision)**: V134–V146 registered from the external `spark-research_v0.8_review.md` full-source review, each independently re-verified against `main`@`644cccd` before registration. Full remediation sequencing lives in `docs/DEVELOPMENT_PLAN_v0.8.1.md`; this table only tracks disposition.

| V134 | LLM provider timeout does not cover streamed/body reads (`anthropic.ts:278-303,372`, `openai_compat.ts:207-228`) — abort timer is cleared right after `fetch()` resolves, so a 200-OK response that then hangs mid-stream wedges the agent indefinitely. | Highest-severity item in the review. Disposition: **v0.8.1 Gate H-1**. |
| V135 | HTTP `compute approve`/`reject` (`server/routes/compute.ts:235-283`) accept any non-empty `actor` with no one-time approval token, asymmetric with `lab approve`/`simulate` (V95, already token-gated). | Disposition: **v0.8.1 Gate H-2** — extend the same one-time-token gate used by `lab`. |
| V136 | HTTP `lab /reject` has no one-time token, unlike the sibling `/approve` handler in the same file. | Disposition: **v0.8.1 Gate H-3**. |
| V137 | `retryable` error flag (`llm/types.ts`) is set but never read anywhere in `llm/router.ts` — no retry/backoff path exists for transient LLM failures. | Disposition: **v0.8.1 Gate H-4** — bounded exponential backoff gated on `retryable === true`. |
| V138 | `orchestrator.ts` `projectCache` (`:278`, `.set`/`.get` at `:626-629`) is a `Map` with no eviction — unbounded growth in a long-running server, one entry per session forever. | Disposition: **v0.8.1 Gate H-5**. |
| V139 | `raw/sink.ts` uses `Bun.sleepSync(5)` as a lock-wait (`:170`) and re-reads the file tail synchronously on every append/verify (`readLastHash`, `readTail`) — blocks the event loop in a single-process HTTP server. | **Investigated for v0.8.1 Gate H-6, not changed — real architecture conflict found, downgraded rather than force a bad fix.** `RawSink.append()` is *deliberately* synchronous (file header comment: two of the four call sites are in `finally`/wrapper return paths, and sync semantics make "was it recorded" independent of promise-settlement ordering) — making it non-blocking requires an async interface, which would touch all 4 call sites, `MemoryRawSink`, and a lot of tests; that's a dedicated migration, not a Gate H patch item. Separately, `Bun.sleepSync` is already the correct *blocking sleep* primitive (not a spin-loop burning CPU) — there is no narrower fix available that both keeps the sync contract and avoids blocking the thread during genuine lock contention. And the plan's originally-proposed mitigation ("cache the tail offset instead of re-reading it") would be actively wrong: the per-append disk re-read of the last hash is not an oversight, it is **the V91 fix** for multi-process hash-chain corruption (server + CLI both appending to the same file) — removing it reintroduces that bug. Practical severity is also lower than it reads: JS's single-threadedness means two `append()` calls can never race *within* one process (the event loop already serializes them), so the lock/sleepSync path only fires under genuine *cross-process* contention, which is rare and brief. Recommend leaving as-is; revisit only if real contention is observed in practice, as a dedicated async-RawSink design (not a patch). |
| V140 | `PythonKernel` (`kernels/manager.ts:195`) does `JSON.parse(line)` on kernel stdout with no try/catch — any out-of-protocol stdout write desyncs the line protocol and throws unhandled. | Disposition: **v0.8.1 Gate H-7** — guard + resync-or-restart via existing `killAndReset()`. |
| V141 | CI (`.github/workflows/ci.yml`) runs only `unit` + binary smoke; `concurrency`/`timeout`/`integration`/`e2e`/`sdk` never run in CI, and `package.json` has no `test:concurrency`/`test:timeout` scripts. | Disposition: **v0.8.1 Gate H-8**. |
| V142 | `orchestrator.ts` `runResearchLoop()` (`:1097`) and `agents/replan.ts` `runReplanLoop()` (`:188`, called at `orchestrator.ts:1217`) have **zero production callers** — re-verified via repo-wide grep excluding `.test.ts`: only `tests/unit/orchestrator.test.ts`, `replan.test.ts`, `narrative_parity.test.ts` reach them. Production `chat`/`processRequest` never invokes them. The "contract-completion + replan loop" narrative in DESIGN.md/CHANGELOG is not exercised by any shipped path. | **Open decision, not resolved — see `DEVELOPMENT_PLAN_v0.8.1.md` §3.** Two options: (a) wire into production `chat` path (scope of a v0.9 lane, not a patch), or (b) delete + downgrade narrative (v0.8.1-compatible). Awaiting user disposition. |
| V143 | AD-12 gate operates at file granularity ("does the module exist and get imported somewhere"), not symbol granularity ("does this specific exported entry point have a production caller") — this is exactly how V142 went undetected. | Disposition: **v0.8.1**, independent of V142's outcome — extend `narrative_parity.test.ts` with a symbol-level check for the specific claim surface (exports referenced by CHANGELOG.md "done" language). |
| V144 | `ControlRepl` (`kernels/control_repl.ts:3,28-69`) is not a sandbox: `new Function(...)` runs at host UID; `ALLOWED_MODULES` only restricts the `require()` shim, not `process`/`globalThis`/`Bun`/`Function.constructor` reachable from the closure. AD-2 ("kernel never gets credentials") is an API-discipline convention, not an isolation boundary. | Disposition split: (a) narrative correction in docs — **v0.8.1**; (b) real isolation (container/UID/JS sandbox) — **deferred to the hardware-integration gate**, prerequisite for any real Opentrons connection. |
| V145 | `server/app.ts:84-135` validates `Origin` only; inline comments describe a Host+Origin dual check that the code doesn't implement. `Origin` allows any `localhost` port unconditionally (deliberate, documented rationale). Low urgency while bound to `127.0.0.1` only. | Disposition: **v0.8.1** — align doc/comment with actual implementation, or add the `Host` check if the server is ever bound beyond loopback. |
| V146 | Wet-lab safety-gate rules `concentration_limit`/`biosafety` only match same-sentence phrasing, not cross-sentence references (the more common real phrasing). Cross-check against existing V25/V59 before treating as new — may be the same gap re-surfaced by the external review. | Disposition: **deferred to the hardware-integration gate** — hard prerequisite before any real Opentrons connection, independent of v0.8.1 scope. |

## Pending (awaiting external input / user ruling) — merged into §post-v0.3

> D1/D3's rationale is expanded in §post-v0.3; D2 (Semantic Scholar API key) has been reconciled into v0.3 P16.

| # | Item | Waiting on what |
|---|-----|--------|
| D1 | A third simulation-platform adapter (materials computation VASP/LAMMPS? EDA?) | Driven by a real user research task; the SimulationPlatform contract has already been validated by two implementations |
| D2 | Semantic Scholar API key integration | User needs to apply for a free key (the connector goes through the credential service, id `semanticscholar`) |
| D3 | Real CNKI / Wanfang API | Whether the user has an API channel; AMiner is currently the main path for Chinese-language literature |

