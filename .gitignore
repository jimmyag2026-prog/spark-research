node_modules/
dist/
*.sqlite
*.sqlite-journal
workspaces/
.pytest_cache/
__pycache__/
*.pyc
# `.venv/` 只匹配目录；worktree 里 .venv 是指向主仓 venv 的符号链接，要去掉斜杠才匹配得上。
.venv
.DS_Store
logs/
.env

# P2: PDF 本体不入库（只有 fixture 里的截断样本进 git）
papers/
*.pdf

# P7: Playwright 产物（trace/截图/报告）不入库
test-results/
playwright-report/
